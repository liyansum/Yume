# Yume 项目总则与开发前设计约束

> 文档状态：开发版本总则 / 开发与发布门禁  
> 最后核对：2026-08-26  
> 适用范围：Yume 仓库中的产品、设计、客户端、引擎移植、测试、发布与文档工作  
> 当前阶段：开发版本。可在已确认边界内创建工程、实现共享基础设施与已通过门禁的功能；仍未确认的功能不得擅自实现，引擎未达到合法样本、许可、安全、性能和兼容矩阵门禁前不得对外宣称支持或发布。

## 1. 产品定义

Yume 是面向 iPhone 与 iPad 的本地多引擎游戏兼容运行器。首版用户从 App 内“+”和系统文件选择器导入自己合法持有的游戏包，Yume 在设备本地完成解包、游戏根目录定位、引擎识别、兼容性分析、必要的非破坏性转换，并通过 App 内静态内置的运行时启动游戏。

首版目标引擎族：

- Ren'Py：覆盖多个兼容代际，而不是在宣传中笼统声称“所有版本”。
- RPG Maker XP / VX / VX Ace：RGSS1 / RGSS2 / RGSS3。
- RPG Maker MV / MZ：HTML5 / JavaScript 运行时。
- ONScripter / NScripter 兼容游戏：首版识别经合法样本验证的 `txt`、`dat` 等脚本或归档形态；`nt2`、`nt3` 暂不加入。文本至少考虑 GBK、Shift-JIS、UTF-8、Big5、EUC-KR。
- Kirikiri 2 / Kirikiri Z 兼容游戏：常见载体包括 XP3。**XP3 是归档格式而不是引擎名称，产品与代码中统一称为 “Kirikiri / XP3”。**
- Flash：首版目标同时覆盖 AS1/AS2（AVM1）与 AS3（AVM2），读取 FWS/CWS/ZWS 标准 SWF 载体，通过随 App 静态提交的独立解释/渲染兼容层运行；不使用 Adobe Flash Player、AIR、ANE、ActiveX、浏览器插件或 JIT，也不支持 Stage3D、DRM、原生扩展、摄像头/麦克风、套接字和外部网络。
- TyranoScript：首版以浏览器导出的 v4/v5 兼容带为目标，使用系统 `WKWebView` 与受限本地 JavaScript/KAG 兼容层；仅支持浏览器兼容纯 JavaScript 插件与安全白名单 shim，不运行 Electron/NW.js/Node 原生模块，不联网或加载原生插件。
- 首版导入 ZIP 与 7z，支持无密码包及用户提供密码的加密包；具体压缩与加密算法以验证矩阵为准。RAR/RAR5 暂不支持。见 ADR-0027（见第 13 节） 与 ADR-0028（见第 13 节）。

非目标：

- 不内置、不托管、不提供商业游戏、RTP、素材、密钥、破解补丁或游戏下载站。
- 不绕过 DRM、授权验证、XP3 自定义加密插件或其他访问控制。
- 不运行 Windows 可执行文件、DLL、ActiveX、DirectShow、NW.js 原生模块或任意下载的原生插件。
- 不承诺仅凭扩展名兼容所有 AVI、RMVB、MP4、MKV；容器、视频编码、音频编码和字幕能力必须分别声明。
- 首版字幕仅支持 Apple 系统媒体框架直接识别的嵌入字幕，不自行解析、加载或烧录外部字幕文件。
- 首版不做云端游戏目录、社区分享、在线下载、账号和社交功能。

## 2. 不可违反的平台边界

1. 所有引擎、解释器、解码器与转换器必须在提交审核的 App 二进制中静态存在。禁止下载新的 dylib、framework、解释器版本、原生插件或可改变 App 功能的代码。
2. 游戏内容只能在 App 容器及用户明确授权的文件范围内读写。导入后默认复制进 Yume 容器，不直接修改源文件。
3. 禁止 JIT、可执行内存、子进程、动态装载用户 DLL、`dlopen` 导入代码、私有 API 和绕过代码签名。
4. Python、Ruby、TJS、ActionScript 与 JavaScript 游戏脚本只能在受限兼容层内运行；不得向脚本暴露任意 iOS API、Objective-C/Swift 反射桥、网络、相册、通讯录、定位、麦克风、摄像头、剪贴板、任意文件路径或进程能力。
5. MV/MZ 使用系统 `WKWebView` / WebKit JavaScript，不打包 Chromium 或 NW.js。默认断网、阻止外部导航、禁用非白名单 URL scheme，并将网页内容与原生桥隔离。
6. 任意游戏若请求系统隐私权限，必须由 Yume 逐次解释用途并取得明确同意；首版原则上不向游戏暴露此类权限。
7. 不得把“自动转换”实现为静默修改、解密或覆盖用户原始游戏。所有转换均写入派生目录，可撤销、可重建、可解释。
8. App Store 审核材料中应主动说明：用户只导入自己拥有权利的内容；运行时均随 App 提交；脚本沙箱不暴露原生 API；没有内置游戏下载或规避保护措施。

相关依据（实现和送审前必须重新核对最新版本）：

- [App Review Guidelines 2.5.2、4.7、5.2](https://developer.apple.com/app-store/review/guidelines/)
- [Apple：在自定义视图中应用 Liquid Glass](https://developer.apple.com/documentation/SwiftUI/Applying-Liquid-Glass-to-custom-views)
- [Ren'Py License](https://www.renpy.org/doc/html/license.html)
- [RPG Maker MV iOS / HTML5 说明](https://rpgmakerofficial.com/product/MV_Help/page/01_11_06.html)
- [RPG Maker MZ 输出格式](https://rpgmakerofficial.com/product/MZ_help-en/01_11_03.html)

## 3. 开发前硬门禁

以下事项必须由产品负责人作出书面选择，并直接记录到本文件正文及第 13 节合并档案。代理或开发者不得自行猜测：

### G1. 开源与商业模式

**已确认（2026-08-26）：** 为支持商业分发，Yume 选择放弃 GPL 组件并独立实现相关兼容能力；不下载或动态加载新引擎、解释器、原生插件、framework、dylib 或 DLL。完整边界与影响见 ADR-0001（见第 13 节）。

> **2026-08-26 更新：** 负责人已改选原三路径中的第一项——整体 GPL 化并引入既有实现，同时放弃 App Store/TestFlight 分发。本节及 ADR-0001 的"放弃 GPL"结论由 **ADR-0053** 取代；静态编译、禁 JIT、禁运行时下载的边界继续有效。

Yume 自有代码采用闭源商业分发；这不减少第三方许可证、NOTICE、署名、源码提供或其他合法义务。商业发布前必须完成专业法律复核。

允许使用经过许可证审计的 Apple 系统框架，以及 MIT、BSD、Apache-2.0、zlib 等宽松许可证组件；所有系统外依赖仍须在引入前完成 ADR、SBOM 和传递依赖审计。首版统一排除 LGPL、AGPL、MPL、EPL 及其他存在 copyleft、文件级公开、替换/重新链接或类似传递义务的组件。

候选的 `mkxp-z` 为 GPL-2.0-or-later，ONScripter 为 GPL-2.0。它们若与 Yume 链接并作为一个 App 分发，可能使整个组合程序承担 GPL 源码与再分发义务；App Store 条款与具体 GPL 履约方式还需法律审查。

该门禁原有三个路径；现已确定采用第三项：

- Yume 采用 GPL 兼容许可证并公开满足要求的完整对应源码、修改、构建方式和许可证；或
- 为 GPL 组件取得可用于闭源 App Store 分发的单独商业许可；或
- 放弃相关 GPL 组件，采用许可兼容的独立实现。

禁止以“拆成 framework”“代码放在不同文件夹”或“只做桥接”为由规避许可证判断。必须建立完整 SBOM，并逐项审核传递依赖、字体、soundfont、FFmpeg 配置及素材许可。

参考上游：

- [mkxp-z（GPL-2.0+）](https://github.com/mkxp-z/mkxp-z)
- [ONScripter（GPL-2.0）](https://github.com/ogapee/onscripter)
- [Kirikiri SDL2（核心源码 MIT，仍须逐项检查依赖）](https://github.com/krkrsdl2/krkrsdl2)

### G2. 最低系统与设备范围

已确认范围：

- 建议最低 iOS / iPadOS 18，以扩大设备覆盖；在 iOS 26 使用原生 Liquid Glass，在旧系统使用材质降级。若产品要求“仅 iOS 26”，须明确接受用户规模缩小。
- iPhone 与 iPad 是否首发同时支持；建议同时支持，且不得把 iPhone 界面简单放大。
- 支持方向：资料库随设备布局；游戏默认服从原作比例并允许单独锁定横/竖屏。
- **已确认（2026-08-26）：** 采用上述方向与比例策略，见 ADR-0005（见第 13 节）。
- 最低设备内存和芯片基线；应以真机压力测试确定，不凭模拟器决定。
- **已确认（2026-08-26）：** 不设置高于系统要求的硬件门槛，支持 Apple 官方可运行 iOS/iPadOS 18 的全部设备，见 ADR-0006（见第 13 节）。

### G3. 引擎兼容矩阵

必须把“支持”改写成可测试的版本矩阵：运行时版本/代际、脚本版本、存档、输入、音频、视频、字体、插件、已知不支持项、测试游戏（必须拥有测试授权）。没有矩阵的引擎不得出现在 App Store 宣传中。

Ren'Py 尤其需要先做双代际技术验证：

- 旧代际：Ren'Py 6/7 与 Python 2 脚本兼容带。
- 新代际：Ren'Py 8 与 Python 3 脚本兼容带。
- “多版本”优先解释为少量经过验证的兼容带，不默认把每个 Ren'Py 发行版都塞入 App。
- 若确需多个 `libpython` 同进程，必须验证符号重命名、全局状态隔离、第三方扩展、包体积和审核风险；验证失败则收缩范围，不以不安全动态加载补救。
- **已确认（2026-08-26）：** 首版目标为 Ren'Py 7.x 与 8.x 兼容带，暂不支持 6.x；精确小版本以验证矩阵为准。见 ADR-0007（见第 13 节）。

### G4. 内容、年龄与审核策略

首版采用“纯本地导入”，不提供游戏下载目录，可显著降低内容运营风险，但不能免除版权、年龄分级和模拟器规则责任。上线前需通过 App Review 预沟通或至少准备详细 Review Notes，确认规则 4.7 对用户本地导入脚本游戏的适用方式。

已确认在大规模引擎开发投入前主动向 App Review 预沟通规则 4.7；在取得并记录回复前不得假定 Apple 已认可当前方案。历史暂缓记录 ADR-0041 已由第 12.1 节第 5、6 项取代。

已确认的产品边界与仍需执行的上架材料：

- App 年龄分级及成人内容处理方式。
- 本地游戏的年龄提示、隐藏/锁定机制是否需要首版提供。
- 用户权利声明、隐私政策、开源许可页、支持网站与侵权联系渠道。
- App 名称 “Yume” 的商标和 App Store 重名检索结果。
- App Store 元数据不得未经授权使用 Ren'Py、RPG Maker、作品名称、封面或厂商 Logo；兼容性商标措辞需法律审核。
- 首发目标为除中国大陆 App Store 之外的全部可用国家和地区；逐地区复核成熟内容、商标、消费者、隐私和当地法律。需要额外排除地区时必须记录原因并由负责人确认。

**已确认并修订（2026-08-26）：** App Store 版本允许合法的成熟主题游戏，但不支持露骨色情、违法内容或其他违反上架规则的内容；Yume 不内置、分发、推荐或提供下载入口。见 ADR-0031（见第 13 节）。

**已确认（2026-08-26）：** 首版不提供成人内容手动标记、封面/标题隐藏或 App 内生物识别/密码锁，也不进行自动成人内容识别。见 ADR-0017（见第 13 节）。

**已确认（2026-08-26）：** 年龄分级按提交时各地区有效规则和实际能力如实申报，不预先硬编码可能变化的等级数字。见 ADR-0018（见第 13 节）。

### G5. 媒体与归档能力

首版媒体方案已确定为：

- 原生直放：优先 AVFoundation 支持的 QuickTime / MPEG-4 等容器与设备硬解编码。
- 自带解码：首版不为系统不支持的媒体加入额外解码器。
- 导入期转换：首版不对系统不支持的音视频进行设备端转码。
- AVI/MKV 只是容器；RMVB 常需额外 RealVideo/RealAudio 解码能力。不得宣传“支持该扩展名”而忽略内部 codec。
- FFmpeg 若使用，必须冻结 configure flags、禁用不允许的 GPL/nonfree 组件、生成许可证清单并完成 LGPL/GPL 合规审查。

ZIP/7z 库同样必须验证加密算法覆盖、许可证、内存模型与 iOS 真机表现。

密码归档目标为 ZipCrypto、WinZip AES-128/192/256 与 7z AES-256。ZIP 与 7z 可分别采用不同的、经审计的宽松许可证或商业许可静态组件；禁止为了统一 API 引入 copyleft 组件或降低安全性。

**已确认（2026-08-26）：** 首版仅播放系统框架能够稳定解码的媒体；RMVB 与系统不支持的罕见 codec 暂不支持，不内置额外解码器或进行设备端转码。见 ADR-0010（见第 13 节）。

## 4. 建议总体架构

采用“原生壳 + 共享服务 + 静态引擎适配器”的单 App 架构。引擎彼此独立，但共享有限且稳定的宿主协议。

```text
SwiftUI App Shell
├── Library / Import UI / Settings（内含 About）/ Diagnostics
├── Application Layer
│   ├── LibraryService
│   ├── ImportCoordinator
│   ├── CompatibilityService
│   └── PlaySessionCoordinator（同一时刻仅一个活跃会话）
├── Core Services
│   ├── ArchiveService + SafeExtraction
│   ├── DetectionService + DetectorRegistry
│   ├── ConversionPipeline + RecipeRegistry
│   ├── GameStorage + SaveStorage
│   ├── MediaService / TextEncodingService / FontService
│   └── Input / AudioSession / Logging / Metrics（仅本地，默认无遥测）
└── Engine Host ABI
    ├── RenPyLegacyAdapter
    ├── RenPyModernAdapter
    ├── RGSSAdapter
    ├── RMMVAdapter / RMMZAdapter
    ├── ONSAdapter
    ├── KirikiriAdapter
    ├── FlashAdapter
    └── TyranoScriptAdapter
```

### 4.1 宿主协议

宿主协议必须是小而稳定的 Swift/C ABI，不允许各引擎直接访问 App 全局状态。建议概念接口：

```swift
protocol GameEngineAdapter {
    static var descriptor: EngineDescriptor { get }
    func probe(at root: URL) async throws -> ProbeResult
    func prepare(context: EngineContext) async throws -> PreparedGame
    @MainActor func makePlayer(for game: PreparedGame) throws -> EnginePlayer
}

protocol EnginePlayer: AnyObject {
    var events: AsyncStream<EngineEvent> { get }
    func start() async throws
    func pause()
    func resume()
    func stop() async
}
```

`EngineContext` 只提供虚拟文件系统、独立存档目录、安全媒体接口、渲染目标、音频、输入、时钟和结构化日志。引擎不能拿到任意容器根路径。

### 4.2 引擎隔离

- iOS 不允许为每个引擎创建独立进程，因此这里的“独立”是模块、状态、目录、队列、资源生命周期和 ABI 隔离，不是安全意义上的进程沙箱。
- 所有 C/C++ 依赖默认隐藏符号；存在 SDL、FFmpeg、Python、Ruby 等版本冲突时使用统一版本或构建期符号前缀，禁止依赖运行时碰运气。
- 一次只允许一个引擎会话；退出后必须销毁渲染器、停止音频、释放纹理/缓存并恢复系统状态。
- 引擎崩溃无法由进程隔离兜底，因此每个适配器必须做输入验证、异常边界和资源上限；尽可能在启动前离线探测不兼容项。
- 每个引擎独立构建为可审计的静态 XCFramework 或静态库；它们仍然属于同一组合程序，不能借此规避许可证。

### 4.3 各引擎建议方案

| 引擎 | 建议运行方式 | 必须验证的高风险项 |
|---|---|---|
| Ren'Py 旧代际 | 基于官方 iOS 移植思路，静态 Python 2 兼容运行时 + SDL/渲染层 | 多运行时符号、Python 扩展、平台条件脚本、视频、包体、GPL/LGPL 依赖 |
| Ren'Py 新代际 | 静态 Python 3 运行时，按验证版本冻结 | Ren'Py 版本兼容、架构相关 `.pyc`、原生 Python 扩展、脚本 API 沙箱 |
| XP/VX/VX Ace | 独立实现 RGSS1/2/3 兼容层与静态白名单 Win32API shim；不得引用 mkxp-z 或加载原 DLL | Ruby 语义兼容、多版本脚本差异、Win32API 白名单、RTP、字体、MIDI、渲染适配 |
| MV/MZ | 系统 `WKWebView` + 本地资源映射 + 受限 CommonJS/静态白名单 NW.js/Node shim | 纯 JS 插件、WebAudio、WebGL、API 白名单、联网/弹窗、大小写路径 |
| ONS | 独立实现许可兼容的 NScripter/ONS 脚本与资源兼容层，内部统一 Unicode | 命令方言、格式资料来源、加密归档、编码误判、旧视频/音频依赖 |
| Kirikiri 2 / Z（XP3） | 独立实现受限 TJS/KAG 与标准 XP3 兼容层 | 自定义 `.tpm`/插件、XP3 过滤器/加密、Windows API、DirectShow、编码与字体 |
| Flash / SWF | 独立的无 JIT SWF/ActionScript 解释与渲染兼容层，随 App 静态提交 | SWF 版本、AS1/2/3、AVM1/2、字节码验证、滤镜/音视频、性能、专有格式与测试样本 |
| TyranoScript | 系统 `WKWebView` + 只读本地资源映射 + 受限 Tyrano/KAG/JavaScript shim | 版本差异、插件、NW.js/Electron/Node 依赖、大小写路径、存档、媒体与字体 |

任何运行时遇到 Windows 原生插件时，默认结果为“明确不兼容”，除非该 API 已由 Yume 合法、静态、可测试地重实现。不得加载原 DLL。

RGSS 首版覆盖 XP、VX、VX Ace 三代，并允许静态实现经验证的 Win32API 白名单；未知 API 和原 DLL 不支持。兼容层不得提供通用 FFI 或暴露 iOS API，详见 ADR-0030（见第 13 节）。

MV/MZ 同为首版目标。支持浏览器兼容纯 JavaScript 插件、受限 CommonJS 与静态白名单 NW.js/Node API shim；不嵌入真实 NW.js/Node 运行时，不加载 `.node`、DLL 或其他原生插件。详见 ADR-0032（见第 13 节）。

Kirikiri 自定义 XP3 加密、过滤器和原生插件暂不支持。可保留只供编译期静态实现使用的内部策略接口，但不得演变为运行时插件或解密模块加载机制；详见 ADR-0009（见第 13 节）。

Kirikiri 2 与 Kirikiri Z 同为首版目标，范围为经验证的标准 TJS/KAG 与未自定义加密 XP3；详见 ADR-0034（见第 13 节）。

Flash 与 TyranoScript 已加入首版目标。Flash 同时覆盖 AVM1/AVM2 与 FWS/CWS/ZWS，但排除 Stage3D、AIR/ANE、DRM、原生扩展、隐私硬件、套接字和外部网络；`SharedObject` 映射到单游戏本地存档。TyranoScript 目标为浏览器导出的 v4/v5，仅支持纯 JavaScript 插件与安全白名单 shim，不运行真实 NW.js/Electron/Node 或原生插件。精确小版本与功能声明仍以合法夹具矩阵为准。

RGSS 首版只运行资源完整的游戏，不支持外部 RTP 依赖；Yume 不内置、下载或托管 RTP。Windows 包内的 `.exe`/启动器只作为识别证据，永不执行；Yume 直接使用可识别的数据目录。

## 5. 导入、识别与转换

### 5.1 导入状态机

```text
选择文件 → 安全检查 → 计算空间 → 临时区解包 → 搜索候选根目录
→ 引擎评分/冲突确认 → 兼容性扫描 → 生成转换计划
→ 用户确认（若耗时或明显增容）→ 派生转换 → 原子提交资料库
```

每一步都必须可取消；失败时清理本次 staging，不能留下半个游戏。大型操作使用 `Progress` 并展示阶段、已处理大小、预计额外空间和可理解的错误。

资料库游戏数量不设固定上限。导入因空间不足、取消或错误中断时，必须及时清理本次任务在 staging 中产生的全部文件；若 App 被系统终止，则在下次启动时安全恢复或清理。清理不得触碰已提交游戏与存档，详见 ADR-0013（见第 13 节）。

### 5.2 安全解包

归档处理必须防御恶意或损坏输入：

- 首版只自动解开用户在文件选择器中明确选择的顶层 ZIP/7z。不得把其中的通用 ZIP/7z 当作容器继续递归展开；内层归档仅由已审计的对应引擎资源读取器按格式需要处理，或留给用户另行导入。
- 拒绝绝对路径、`..` 越界、符号链接逃逸、设备文件、硬链接逃逸和异常文件名。
- 解包前检查磁盘空间；设置单文件、总展开大小、文件数量、路径长度、压缩比、解析耗时和内存上限，以防 zip bomb / 7z bomb。阈值必须根据最低支持设备压力测试制定、版本化并随 App 更新，属于用户不可关闭的强制安全策略。
- 单游戏不设固定 GB 上限，但必须依据实际可用空间、派生与临时空间需求和系统安全预留量实施动态预算；初始安全预留采用 `max(2 GiB, 设备容量的 5%)` 且最高 10 GiB，之后以真机测试校准。“无固定上限”不豁免上述压缩炸弹防护。见 ADR-0011（见第 13 节）。
- 不把密码写入日志。首版不提供“记住密码”；密码只在本次导入任务内存中保存，任务结束后立即清除，不写入 Keychain 或磁盘。见 ADR-0027。
- 密码失败与归档损坏应使用不同错误；不做暴力枚举。
- staging 与最终目录必须同卷，成功后以原子移动提交。
- ZIP/7z 成功导入后删除 Yume staging 中的归档临时副本，只保留解包后的不可变原件；不触碰用户外部源归档。见 ADR-0046。
- 归档内若只有单一包裹目录，应继续向下寻找；搜索深度和候选数有上限。

### 5.3 引擎识别

采用可扩展 `GameDetector` 注册表。检测器返回分数、证据、候选根目录、精确代际和警告，不以单一扩展名作结论。

典型证据（实现前用真实合法样本校正）：

- Ren'Py：`game/`、`.rpa`、`.rpyc`、Ren'Py runtime 目录或启动信息。
- RPG Maker XP：`Game.ini` + `Data/Scripts.rxdata`；VX 为 `.rvdata`；VX Ace 为 `.rvdata2`。
- MV：`www/js/rpg_core.js`、`www/data/System.json`，或去掉 `www` 包装后的等价根目录。
- MZ：`js/rmmz_core.js` + `data/System.json`。
- ONS：`0.txt`、`00.txt`、`nscript.dat` 及对应 NSA/SAR/NS2/NS3 等资源证据；首版遇到 `nt2`、`nt3` 应明确报告暂不支持。
- ONS 首版范围确定为 `0.txt`、`00.txt`、标准 `nscript.dat` 及经验证的 NSA/SAR/NS2/NS3；自定义方言或加密不因扩展名相同而自动纳入。见 ADR-0033（见第 13 节）。
- Kirikiri：`data.xp3`、`startup.tjs`、`system/`、KAG/TVP 特征；单个任意 `.xp3` 不足以判定完整游戏。
- Flash：SWF 魔数 `FWS`/`CWS`/`ZWS`、文件版本、舞台信息和 ActionScript/AVM 特征；单个扩展名错误或损坏的 `.swf` 不足以判定可运行。
- TyranoScript：`data/system/Config.tjs`、`data/scenario/`、`tyrano/`、`tyrano.ks`、`index.html` 和 TyranoScript 运行时/插件证据；必须区分浏览器版与 NW.js/Electron 包装依赖。

若多个候选接近，展示“发现多个游戏/无法确定引擎”界面，由用户选择，禁止静默选错。一个归档包含多个游戏时，应允许分别导入。

低置信度的引擎、根目录或编码识别必须展示证据与预览并由用户选择（ADR-0049）。一个归档包含多个游戏时允许一次多选并分别提交（ADR-0048）；检测到重复游戏时提供取消、保留两份或替换，替换默认保留存档（ADR-0047）。

### 5.4 编码识别

- 先用 BOM、格式魔数和引擎/语言先验，再用严格解码与统计评分；禁止把通用编码探测器的第一结果当事实。
- 候选至少包括 UTF-8、Shift-JIS/CP932、GBK/GB18030、Big5、EUC-KR；内部文本统一转为 Unicode，但原件不变。
- 导入预览显示检测结果、置信度和乱码片段，低置信度时让用户手动选择。
- CP932 与标准 Shift-JIS、GBK 与 GB18030 不得在实现中错误地视为完全相同。

### 5.5 非破坏性转换

转换使用版本化 `ConversionRecipe`：输入哈希、引擎版本、步骤、工具版本、输出哈希、警告与日志均记录在 manifest。典型步骤：

- 大小写不敏感路径索引或确定性的路径修复映射。
- 文本编码到 UTF-8/内部 Unicode 的派生副本。
- 不受支持媒体转为设备友好格式。
- 图片格式/色彩空间转换、字体回退、MIDI 到受许可 soundfont 的播放方案。
- **首版例外：** MIDI 暂不支持，不内置或下载 soundfont；检测到 MIDI 依赖时应明确报告兼容影响。见 ADR-0037（见第 13 节）。
- 已知且可证明等价的配置补丁或兼容 shim。

未经专门测试，禁止自动改写游戏脚本语义。转换前后保留原文件；用户可查看“修改了什么”、清除派生缓存并重新生成。媒体转换必须保留时间轴、循环点与音视频同步，失败则标记局部不兼容，不能假装成功。

耗时、明显增容或可能影响表现的转换必须先合并展示步骤、原因、时间、空间和影响并取得确认；轻量、确定性、可撤销且不改变语义的处理可自动执行，但仍须记录 manifest。见 ADR-0045（见第 13 节）。

派生转换文件不设固定容量上限，但每个步骤必须先通过统一动态空间预算并写入任务专属临时目录；失败时只清理本次未提交结果。详见 ADR-0014（见第 13 节）。

同一时间只运行一个解包或转换重任务，其余重任务排队；文件头探测、元数据读取等轻量工作可在统一资源预算下受控并发。低空间时只自动清理未提交的 staging 与普通缓存；已完成的派生文件须经用户确认后才可删除，游戏原件与存档永不自动删除。

## 6. 数据模型与目录

建议容器结构：

```text
Application Support/Yume/
├── Games/<game-id>/
│   ├── original/       # 导入后的不可变副本
│   ├── derived/        # 可删除、可重建的转换结果
│   ├── saves/          # 用户存档，独立备份策略
│   ├── manifest.json   # 类型、证据、哈希、版本、转换记录
│   └── logs/           # 有大小与保留期限制，用户可导出
├── Cache/              # 系统可清理内容
└── Library.sqlite      # 或经验证后的 SwiftData 存储
```

- `original/` 在 Yume 内视为只读；升级引擎不得迁移或覆盖原件。
- 所有导入均完整复制到 Yume 容器，成功后不依赖源位置；不修改源文件，容器内 `original/` 也保持不可变，所有处理写入 `derived/`。见 ADR-0044（见第 13 节）。
- 存档与派生缓存分开，清理缓存绝不能删除存档。
- 删除游戏前分别列出游戏本体、派生文件与存档大小；默认保留存档，只有用户主动改选并确认后才同时删除，同时删除前提供离线导出入口。见 ADR-0043（见第 13 节）。
- 游戏原件、派生文件和缓存排除系统设备备份；存档与设置可进入用户自行启用的加密设备备份。这不属于 Yume 主动云同步，见 ADR-0042（见第 13 节）。
- 首版不使用 iCloud 或其他云服务同步任何数据；存档仅保存在本机，可由用户主动离线导入/导出。见 ADR-0019（见第 13 节）。
- 数据库只是索引，必要时可从 manifest 重建。
- 资料库总容量不设固定 GB 上限，仅受统一的动态空间预算和系统安全预留量限制；详见 ADR-0012（见第 13 节）。

## 7. UI / UX 设计约束

`demo.png` 仅作为视觉方向参考，不要求像素复刻。目标是“安静、沉浸、清晰的私人游戏资料库”，而不是堆满半透明卡片。

### 7.1 视觉语言

- 以系统 SwiftUI 导航、Tab、菜单、搜索和 Sheet 为基础；iOS 26 优先让标准组件自然获得 Liquid Glass。
- 自定义玻璃只用于导航、浮动操作、播放器控制等交互层。内容卡片优先使用低层级实色/材质表面，避免每层都模糊造成过度绘制和文字对比不足。
- 背景可从当前封面提取低饱和环境色，但必须保证滚动性能并尊重“减少透明度/减少动态效果”。
- 游戏可在对应会话内使用其自带 TTF/OTF；缺失或不兼容时回退到经过验证的系统字体链。不下载字体，也不内置来源不明字体。见 ADR-0038（见第 13 节）。
- 支持浅色、深色、高对比度、Dynamic Type、VoiceOver、Switch Control、外接键盘和游戏控制器。
- 所有点击目标至少 44×44 pt；状态不能只靠颜色表达。

### 7.2 信息架构

首版主导航确定为两项：

1. **游戏库**：搜索、筛选、排序、列表/网格、继续游戏。
2. **设置**：存储、默认控制、兼容性、日志、开源许可、隐私、关于。

“关于”确定移入“设置”，不占一级 Tab。导入/转换任务通过导航栏进度入口进入，无活跃任务时可隐藏，不保留常驻任务 Tab。详见 ADR-0040（见第 13 节）。

### 7.3 游戏库

- 资料库默认封面网格并可切换列表，偏好按设备本地保存；iPhone 使用紧凑列表/双列网格，iPad 使用自适应多列网格与可选详情侧栏。见 ADR-0052。
- 封面只读取游戏包内图片或由用户从本地明确选择；禁止联网抓取。没有合适图片时使用 Yume 自有且许可清楚的占位图。
- 卡片首要信息是封面/占位图、标题、引擎精确标签、最近游玩；文件大小和导入日期降为次级信息。
- 显示兼容状态：可运行、需转换、部分兼容、无法运行，并可点开证据和解决建议。
- 不使用未经授权作品封面做演示或商店截图；内置 demo 必须完全自有或许可清楚。

### 7.4 导入体验

- 首版入口为导航栏“+”和系统文件选择器；Share Extension 暂缓，不创建扩展 target 或共享容器。见 ADR-0039（见第 13 节）。
- 首次发起导入前仅一次确认用户只导入自己合法持有且有权使用的内容；声明实质变更前不重复打扰，全文在“设置 → 关于/法律”持续可见。见 ADR-0029（见第 13 节）。
- 选择后立即进入可持续观察的任务页，不用阻塞弹窗。
- 加密包在需要时询问密码；低置信度编码、多个根目录、多个引擎候选、超大转换必须请求用户决定。
- 错误信息包含：发生阶段、可理解原因、是否保留原件、可采取动作、可导出的诊断编号；不得只显示引擎 stderr。

### 7.5 游戏详情与播放器

- 详情页：启动/继续、兼容报告、存档管理、控制布局、显示比例、单游戏引擎设置、文件与缓存占用、重新扫描、删除。
- 所有公开支持的引擎均须通过存档/读档往返测试；提供单游戏存档的用户主动离线导入与导出，不承诺未经验证的跨平台兼容。见 ADR-0035（见第 13 节）。
- 启动时显示引擎与兼容层版本；首次存在警告时先说明，不反复打扰。
- 播放器按原始分辨率等比缩放，默认不裁切；安全区以黑边/环境背景处理。
- 首版支持直接触控、虚拟摇杆/按键、外接键盘和 MFi/系统游戏控制器。触控覆盖层按引擎配置：视觉小说提供点击、快进、历史、自动；RPG 提供方向、确认、取消、菜单。允许调透明度/位置/尺寸，并按游戏保存布局及键盘/控制器映射；支持恢复默认。见 ADR-0036（见第 13 节）。
- 系统手势、Home 指示条、键盘、画中画和音频中断行为应由统一宿主管理。
- 同一时间只允许一个活跃游戏会话；启动其他游戏前必须安全停止并释放当前会话。锁屏、进入后台或音频中断时自动暂停；首版不提供后台持续运行或画中画。
- iPad 首版只运行一个 Yume 场景，不支持同时打开多个 Yume 窗口；大屏仍使用自适应侧栏/分栏布局。

## 8. 性能、稳定性与安全预算

- 资料库滚动不得加载完整游戏资源；封面使用降采样缩略图和异步缓存。
- 解压、哈希、识别、转码不占主线程；任务受控并发，后台时间到期时安全暂停，不能声称可无限后台转换。
- 后台时间结束前保存检查点并暂停，返回前台后自动恢复；恢复前重新验证空间与任务状态。见 ADR-0050。
- 真机测试热量、峰值内存、首帧、音频中断、来电/锁屏、低存储和大型归档。
- 每个引擎建立资源预算与 watchdog：纹理尺寸、单次分配、脚本递归、日志速度、打开文件数、转换临时空间。
- 游戏输入一律视为不可信。解析二进制数据时使用边界检查与 fuzz 测试；优先选择仍维护并有安全记录的依赖。
- 日志始终只保存在本地，不记录密码、完整用户路径、完整脚本文本、存档正文或个人数据。每游戏日志最多 20 MB、最多保留 7 天，且全 App 日志设 200 MB 硬上限；超过任一限制时滚动淘汰最旧记录。用户主动导出前展示脱敏后的内容。
- 当前开发版本启用详细结构化日志：记录时间、级别、子系统、任务/会话 ID、引擎与兼容层版本、探测证据、归档/转换阶段、耗时、资源预算、状态转换、脚本控制台警告、错误链和恢复/清理结果。文件路径使用容器相对路径或哈希，脚本仅记录文件标识与行列号，不写正文。开发构建提供“开发者诊断”页面，可实时查看这些日志、任务 manifest、资源预算并本地导出；Release 构建仅保留普通诊断入口和必要的 info/warning/error，不暴露开发者页面。
- 若加入崩溃分析或遥测，需单独产品决策、隐私清单、隐私政策和关闭选项；不得由某个引擎自行联网。
- **已确认（2026-08-26）：** 首版完全离线，无账号、无游戏下载目录、无遥测；游戏脚本和 Web 内容同样禁止联网。见 ADR-0015（见第 13 节）。

## 9. 测试与完成定义

每个引擎至少维护：

- 自有或明确授权的最小公开测试夹具，不把商业游戏提交到仓库或 CI。
- 测试夹具优先由项目自行制作并确保全部权利清楚；无法自行覆盖时取得明确的第三方书面授权。授权范围需分别覆盖仓库、CI、审核演示和公开再分发，详见 ADR-0021（见第 13 节）。
- 立即为每个目标引擎制作完全自有的最小合法夹具；夹具及其授权、哈希和预期结果必须先于对应兼容实现进入仓库。
- 探测、编码、归档与路径处理的单元测试。
- golden frame / 音频事件 / 输入映射 / 存档往返测试。
- 一组不兼容夹具，验证能给出确定且安全的失败，而不是崩溃。
- 对 ZIP Slip、7z bomb、损坏头、极长路径、符号链接、大小写冲突、低存储的安全测试。
- iOS 当前版本与最低支持版本的真机矩阵；至少覆盖一台低基线设备和一台当前设备。

某引擎达到“可公开支持”的最低条件：

1. 兼容矩阵已发布且表述准确。
2. 合法测试集核心流程、存档和媒体通过。
3. 已知不支持项在导入前或启动前可诊断。
4. 依赖许可、源码提供义务、隐私清单与 App Store 审核说明完成。
5. 无任意文件访问、任意原生代码加载或未经同意的网络/权限访问。
6. 崩溃率、内存、发热和启动时间达到产品设定阈值。

## 10. 建议迭代顺序

在门禁通过后，仍应逐步开发，不得一开始并行塞入所有引擎；但商店首版必须等所有已列入首版的引擎均达到发布门禁后再上架：

1. **技术验证**：分别验证 Ren'Py 双代际、RGSS 多 Ruby、WKWebView MV/MZ、ONS、Kirikiri、Flash、TyranoScript 在当前 Xcode/iOS 真机上的最小启动，并测包体/符号/许可证。
2. **宿主骨架**：资料库、存储、导入状态机、检测注册表、宿主 ABI、统一渲染/音频/输入与诊断。
3. **第一条纵向切片**：确定先做 MV/MZ，完成 ZIP/7z 导入、识别、兼容报告、启动、输入、存档、退出和诊断的全流程。
4. **归档与转换**：安全 ZIP/7z、编码确认、媒体能力矩阵、非破坏转换 manifest。
5. **逐引擎接入**：每次只加入一个适配器，并在合入前完成其兼容矩阵与许可审计。
6. **上架准备**：隐私、年龄、商标、开源许可、审核演示内容、Review Notes、支持页、TestFlight 与真机回归。

首个全链路原型完成后，依据实测包体制定内部包体上限；超限时优先收缩未验证资源或实现，绝不改为下载引擎。启动、首帧、峰值内存、发热和稳定性阈值须先在最低 iOS/iPadOS 18 真机建立基准，由工程团队提出具体数值并经负责人一次批准。

外部 TestFlight 开始前，必须完成目标地区品牌检索、公开支持页、隐私政策、侵权联系渠道和第三方许可证页面。品牌未确认前使用临时 Bundle ID 与 “Yume” 开发代号；品牌核验后只进行一次有迁移清单、签名/容器验证和回滚方案的受控迁移。

商店首版不按已完成引擎提前发布：Ren'Py 7/8、RGSS1/2/3、MV/MZ、ONS、Kirikiri 2/Z、Flash、TyranoScript 以及共享导入/存档能力必须各自满足兼容矩阵、合法样本、安全、许可、性能和审核材料门禁。若未来需要收缩首版引擎清单，必须由负责人重新确认并更新本文件。

## 11. 代理与贡献者工作规则

- 仓库根目录的 `Agents.md` 是产品约束、已确认决策和开发门禁的唯一规范来源；`ARCHITECTURE.md` 是技术结构、依赖方向和代码导航的架构基线。开始任务前必须依次阅读 `Agents.md`、`ARCHITECTURE.md`、`progress.md` 与 `status.md`；若更深目录出现局部约束，则同时遵守。两者冲突时以 `Agents.md` 为准。
- 新增、删除或重命名顶层模块、关键协议、持久化结构或核心数据流时，必须同步更新 `ARCHITECTURE.md`；尚未实现的规划必须明确标注，不能让后续代理误认为代码已经存在。
- `progress.md` 只记录项目总体、里程碑和模块级长期状态，包括已完成能力、当前阶段、关键门禁与下一里程碑。只有总体状态变化时才更新，保持精炼，不粘贴实现日志、代码或本文件已有的大段决策。
- `status.md` 只记录当前任务：目标、当前状态、已改文件、关键判断、阻塞、下一步和验证结果。开始新任务时重写为新任务状态，不长期追加历史；长任务在阶段变化时同步，结束前必须更新为完成或明确阻塞。
- Codex 完成任务前应同时核对代码/文档与 `status.md`，若任务改变总体里程碑则同步更新 `progress.md`。两份进度文件只写摘要并引用真实路径，不记录密码、密钥、个人数据或冗长命令输出。
- 为适应 Codex 使用限额和跨会话恢复，任何可能被中断的长任务都必须在 `status.md` 留下可执行检查点：当前目标、已完成步骤、未提交变更、最近验证、阻塞和唯一明确的下一步。阶段变化、准备等待或预计会话即将结束时立即更新，不依赖聊天记录恢复上下文。
- 当负责人在新会话只输入“继续开发”时，Codex 必须读取四份必读文档和当前 Git 状态：若 `status.md` 中有未完成且未阻塞任务，就从其下一步继续；若当前任务已完成，则从 `progress.md` 与 `ARCHITECTURE.md` 选择优先级最高、未受门禁阻止的下一项自主推进。只有遇到需要负责人选择、额外授权、外部凭据或无法安全推断的门禁时才暂停询问。
- GitHub 远端固定为 HTTPS `https://github.com/liyansum/Yume.git`，仓库保持 private，只使用并直接推送 `main`，不创建功能分支或 PR。同步前先检查远端与本地状态、运行与改动风险相称的验证，并确认没有密钥、隐私数据、无权分发的素材或测试游戏。
- 不为每次小修改提交或推送。仅在形成可恢复的关键节点时同步：完成一个总体里程碑、可运行纵向切片、关键架构/数据迁移、经审计依赖变更、发布门禁结论，或会话结束前已有完整且通过验证的实质成果。多个相关小改动合并为一次清晰提交；不推送明知损坏或仅半成品的状态。
- 同步关键节点时直接提交并推送 `main`。推送前执行 `git fetch origin main` 并检查是否分叉；若远端存在本地未知提交，禁止 force push 或改写历史，应先安全整合，无法判断时停止并报告。默认禁止 `push --force`、删除远端历史或公开仓库。
- 当前为开发版本。允许在已确认边界内创建工程和提交实现；未确认事项不得自行猜测，任何引擎/依赖在其许可、合法样本、安全和兼容矩阵门禁完成前不得发布或对外宣称支持。禁止下载运行时引擎或把下载内容作为 App 功能代码。
- 做技术选型时优先官方文档、上游源码和许可证原文；记录链接、版本/commit、访问日期与结论，不以论坛传言作为唯一依据。
- 不得写“全版本支持”“全部游戏兼容”“所有媒体格式”等无法测试的承诺。
- 引入依赖前提交 ADR：用途、替代方案、许可证、维护状态、安全、包体、最低系统、Swift/C ABI、App Store 风险和退出方案。
- 不修改用户原件，不实现 DRM 绕过，不把受版权保护的游戏、RTP、字体、soundfont、封面或密钥加入仓库。
- 所有引擎功能必须通过 `GameEngineAdapter` 边界接入，不允许 UI 直接调用引擎全局函数。
- 共享层不得出现针对单个商业游戏名称的硬编码补丁；兼容补丁应基于可解释的引擎/插件特征，并附授权测试与回归用例。
- 遇到平台规则、许可证、媒体专利或商标不确定时必须停止相关实现并升级确认，不得用技术手段绕过。
- UI 必须使用本地化 key，首发同时交付简体中文、繁体中文、英语和日语；游戏文本的编码支持与 App UI 本地化是两件事。见 ADR-0051。
- 所有关键判断写入可测试的 manifest/descriptor/recipe，不散落成不可审计的条件分支。

## 12. 负责人确认结果与执行门禁

- [x] 为支持商业分发，放弃 GPL 组件并独立实现；不下载或动态加载新引擎及 DLL。见 ADR-0001。
- [x] 一次性买断；首版不采用订阅或功能内购拆分核心能力。见 ADR-0002。
- [x] 最低 iOS/iPadOS 18；iOS/iPadOS 26 使用原生 Liquid Glass，旧系统采用材质降级。见 ADR-0003。
- [x] iPhone 与 iPad 同步首发，并分别提供自适应布局。见 ADR-0004。
- [x] Ren'Py 首版目标为 7.x legacy + 8.x modern，暂不支持 6.x；具体小版本以验证矩阵为准。见 ADR-0007。
- [x] RGSS 首版目标为 XP/VX/VX Ace；支持静态白名单 Win32API 兼容行为，但不加载原 DLL、不支持未知 API。见 ADR-0030。
- [x] ONS `nt2` / `nt3` 暂不加入首版；取得合法资料与样本并验证后再评估。见 ADR-0008。
- [x] Kirikiri 自定义 XP3 加密、过滤器和插件暂不支持；保留编译期内部接口供后续静态实现。见 ADR-0009。
- [x] RMVB 与系统无法原生解码的罕见 codec 暂不支持；首版不做设备端转码。见 ADR-0010。
- [x] 单游戏、总资料库、游戏数量和派生文件均不设固定上限；采用动态空间预算、安全解包阈值及失败残留清理。见 ADR-0011 至 ADR-0014。
- [x] 首版完全离线、无账号、无游戏下载目录、无遥测。见 ADR-0015。
- [x] 允许合法的成熟主题内容，但不支持露骨色情或违法内容；首版无自动内容识别、隐藏或本地锁；年龄分级如实申报。见 ADR-0017、ADR-0018、ADR-0031。
- [x] 首版保持完全离线，不使用 iCloud 同步；存档仅本地保存并支持用户主动离线导入/导出。见 ADR-0019。
- [x] 当前尚未拥有各引擎合法样本；后续优先制作完全自有夹具，无法覆盖时取得第三方书面授权。样本到位前不得发布对应兼容能力。见 ADR-0020、ADR-0021。
- [x] 尚未核验 “Yume” 名称、图标、域名、商标与 App Store listing；核验完成前仅作为内部开发代号，不得视为最终品牌。见 ADR-0022。
- [x] 发布材料目前均暂无：公开支持页、隐私政策、侵权联系渠道、第三方许可证页面及潜在源码义务交付地址；全部属于上架门禁。见 ADR-0023 至 ADR-0026。

### 12.1 批量确认结果（2026-08-26）

1. Yume 自有代码采用闭源商业分发；第三方代码和资产依法展示许可与署名。
2. 首发基准售价等全引擎 TestFlight、包体和竞品调研后确定。
3. App Store 能力允许时开启家庭共享。
4. 一次买断后保持既定权益和价格逻辑：同一 App 的后续更新不再次收费，不另设付费重大版本；既有购买者不因更新产生额外费用。
5. 在大规模引擎开发投入前主动向 App Review 预沟通规则 4.7。
6. 若 Apple 的索引、Universal Links、年龄识别/限制要求与完全离线或安全设计冲突，先评估最小合规调整；若会破坏完全离线与安全底线，则停止 App Store 路线，不绕过审核。
7. 商业发布前必须完成第三方许可、商标、格式权利和 App Store 分发方案的专业法律复核。
8. Flash 首版同时支持 AS1/AS2（AVM1）与 AS3（AVM2）。
9. Flash 首版以 FWS、CWS、ZWS 三种标准 SWF 载体为目标。
10. Flash 明确不支持 Stage3D、AIR/ANE、DRM/受保护内容、原生扩展、摄像头/麦克风、套接字和外部网络。
11. Flash `SharedObject` 映射到单游戏本地存档目录，并纳入存档离线导入/导出。
12. TyranoScript 首版以浏览器导出的 v4/v5 兼容带为目标，精确小版本由合法夹具确定。
13. TyranoScript 仅支持浏览器兼容纯 JavaScript 插件与安全白名单 shim，不支持 Electron/NW.js/Node 原生模块。
14. RGSS 首版不支持外部 RTP 依赖，只运行资源完整的游戏；Yume 不内置、下载或托管 RTP。
15. Windows 游戏包内的 `.exe`/启动器只作为识别证据，永不执行；Yume 直接使用可识别的数据目录。
16. 密码归档目标包括 ZipCrypto、WinZip AES-128/192/256 与 7z AES-256，具体组合以合法夹具验证为准。
17. ZIP 与 7z 可以分别采用不同的、经审计的宽松许可证或商业许可静态组件。
18. 禁止自动递归解开游戏包内部的通用 ZIP/7z；只解用户选择的顶层归档，内层归档交给对应引擎或用户后续决定。
19. 解包文件数、路径、压缩比、耗时和内存等安全阈值由工程团队根据最低设备压力测试制定、版本化并随 App 更新，用户不能关闭。
20. 初始磁盘安全预留采用 `max(2 GiB, 设备容量 5%)`，最高 10 GiB，并由真机测试校准。
21. 同一时间只运行一个解包/转换重任务，其余排队；轻量探测可受控并发。
22. 低空间时只自动清理未提交 staging 和普通缓存；删除已完成派生文件前询问用户，原件与存档永不自动删除。
23. 日志除每游戏 20 MB、最多 7 天外，增加全 App 200 MB 硬上限。
24. 开发构建提供“开发者诊断”页面，可实时查看详细结构化日志、任务 manifest、资源预算并本地导出；Release 只保留普通诊断入口。
25. 首版字幕只支持系统媒体框架直接识别的嵌入字幕，不自行解析或烧录外部字幕。
26. 内部包体上限等首个全链路原型测量后确定；超限时优先收缩未验证资源/实现，不下载引擎。
27. 启动、首帧、峰值内存、发热和稳定性阈值在最低 iOS/iPadOS 18 真机基准完成后由工程团队提出，再由负责人一次批准。
28. 立即制作各引擎完全自有的最小合法测试夹具，并执行“夹具先于对应引擎兼容实现”。
29. 品牌检索、支持页、隐私政策、侵权渠道和许可证页面必须在外部 TestFlight 前完成。
30. 品牌未确认前使用临时 Bundle ID 与 “Yume” 开发代号；核验后允许一次受控迁移。
31. 首发目标为除中国大陆 App Store 之外的全部可用国家和地区；每个地区仍须通过成熟内容、商标、消费者、隐私和当地法律复核。若还需排除其他地区，必须记录原因并由负责人确认。

### 12.2 已知执行门禁（不是产品选择）

- 当前没有任何目标引擎的合法测试/审核样本；必须按第 28 项执行后逐引擎解除门禁。
- 所有实际依赖仍需固定版本/commit、ADR、SBOM、许可证原文、传递依赖、安全记录、包体和退出方案审计。
- “Yume” 仍只是开发代号；名称、图标、域名、商标和 App Store 重名检索尚未执行。
- 支持页、隐私政策、侵权联系渠道、第三方许可页与可能的源码义务交付地址均尚未建立。
- 已决定在大规模引擎开发投入前主动预沟通，但 App Review 规则 4.7 对完全离线、本地导入脚本游戏的具体适用方式仍未获得 Apple 确认。
- Flash、TyranoScript 以及既有每个引擎都尚缺精确版本/功能兼容矩阵、安全夹具和真机性能数据。
- ZIP/7z 的密码算法覆盖与符合许可政策的静态实现尚未选型和验证。
- 最低设备上的动态空间阈值、性能阈值、包体预算与详细硬件测试矩阵尚未测定。
- 商业发布前的许可证、格式权利、商标和分发法律复核尚未完成。

在上述执行门禁完成前，可以实现已确认的共享基础设施和开发版诊断，但不得对外承诺兼容范围或发布商店版本。需要等待原型测量的包体与性能阈值不允许凭空填写，完成测量后按第 12.1 节第 26、27 项提交负责人一次批准。

## 13. 已合并的决策与调研档案

本节完整吸收原 `docs/` 目录中的决策与调研 Markdown。自 2026-08-26 起，本文件是这些记录的唯一仓库内来源；后续决策直接更新本节与相关正文，不再恢复独立 `docs/` 目录。

<!-- 原文档：docs/decisions/0001-commercial-licensing-and-static-runtimes.md -->

### ADR-0001：商业许可边界与静态运行时

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 背景

Yume 计划支持商业分发。候选实现中存在 GPL 组件，同时游戏包可能携带 Windows DLL、原生插件或其他引擎代码。此类组件会带来许可证、App Store 审核、代码签名和安全风险。

#### 决策

1. Yume 不引用、链接、修改或分发 GPL 组件，包括但不限于 `mkxp-z` 和 ONScripter。
2. 相关引擎兼容能力由 Yume 独立实现。实现只能依据有权使用的规范、格式资料、测试样本和许可兼容的代码，不复制或改写 GPL 源码。
3. 允许使用 Apple 系统框架，以及经过许可证审计的 MIT、BSD、Apache-2.0、zlib 等宽松许可证组件。
4. 首版统一排除 GPL、LGPL、AGPL、MPL、EPL 及其他带 copyleft、文件级公开、替换/重新链接或类似传递义务的组件；不得通过静态链接、拆分 framework 或桥接规避。
5. 第三方代码只可在完成 ADR、SBOM 和传递依赖审计后引入；也可使用明确取得且满足分发要求的商业许可组件。不得仅凭“未直接调用”推定许可兼容。
6. 所有随 App 执行的引擎、解释器、解码器和兼容层必须在构建及送审时固定，并静态包含于 App。
7. App 不下载、更新或动态加载新的引擎、解释器、原生插件、framework、dylib 或 DLL，也不执行游戏包携带的原生代码。
8. 遇到依赖 Windows DLL、Win32API、ActiveX、DirectShow、NW.js 原生模块或其他原生插件的游戏，默认报告为不兼容；只有当所需行为已由 Yume 合法、静态、可测试地独立实现时，才可声明支持。

#### 影响

- RGSS 与 ONS 不采用现有 GPL 引擎作为产品实现基础，开发量和兼容验证成本会上升。
- 不以动态下载运行时扩展兼容范围；新增或升级引擎能力必须随新的 App 版本提交。
- 具体实现开始前仍需逐项审计传递依赖、素材、字体、soundfont、媒体编解码器和工具链许可证。
- 本决策不是法律意见；商业发布前仍需由合格法律人员复核许可证与分发方案。

---

<!-- 原文档：docs/decisions/0002-one-time-purchase.md -->

### ADR-0002：一次性买断商业模式

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

Yume 采用一次性买断模式。用户购买 App 后即可使用当前版本提供的完整功能；首版不采用订阅，也不以功能内购拆分引擎或核心能力。

#### 影响

- 产品设计中不设置订阅入口、续费提示或功能解锁页。
- 引擎兼容、安全更新和已承诺能力的维护不能依赖持续订阅状态。
- 首发基准售价等全引擎 TestFlight、包体和竞品调研后确定；确定后保持一次性买断价格逻辑，不设置单独教育折扣、订阅或功能内购。
- App Store 能力允许时开启家庭共享。同一 App 的后续更新不再次收费，不另设付费重大版本；既有购买者不因更新产生额外费用。

---

<!-- 原文档：docs/decisions/0003-minimum-os-version.md -->

### ADR-0003：最低系统版本

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

Yume 的最低系统版本为 iOS 18 和 iPadOS 18。

#### 影响

- 所有产品功能和引擎兼容层必须能够在 iOS/iPadOS 18 真机上运行。
- iOS/iPadOS 26 使用系统原生 Liquid Glass；18 至 25 使用可用性检查和系统材质降级，不复制仅在 26 提供的 API 行为。
- CI、真机测试矩阵和部署目标均以 18 为最低基线；不得因开发设备版本较新而无意提高部署目标。

---

<!-- 原文档：docs/decisions/0004-iphone-ipad-simultaneous-launch.md -->

### ADR-0004：iPhone 与 iPad 同步首发

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

Yume 在 iPhone 与 iPad 上同步首发。

#### 影响

- 两类设备必须共享核心能力、引擎兼容范围和购买权益，不把 iPad 支持延后。
- UI 采用尺寸类别和可用空间驱动的自适应布局；iPad 使用适合大屏的多列资料库、侧栏或详情布局，不简单放大 iPhone 界面。
- 测试矩阵必须同时覆盖 iPhone 与 iPad 的最低系统和当前系统真机。

---

<!-- 原文档：docs/decisions/0005-orientation-and-aspect-ratio.md -->

### ADR-0005：屏幕方向与游戏画面比例

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 资料库、任务和设置等 App 界面跟随设备方向及可用空间自适应布局。
2. 游戏默认保持原作画面比例，不拉伸、不裁切。
3. 用户可以为每个游戏单独锁定横屏或竖屏；单游戏设置覆盖默认方向策略。

#### 影响

- 播放器需统一处理安全区、黑边或环境背景，以及旋转期间的渲染与输入坐标变换。
- 单游戏方向选择需要持久化，并在启动、退出及系统中断后正确恢复。
- iPhone 与 iPad 均需测试横竖屏切换、方向锁定、外接显示和多任务尺寸变化。

---

<!-- 原文档：docs/decisions/0006-all-ios-18-compatible-devices.md -->

### ADR-0006：支持全部 iOS/iPadOS 18 兼容设备

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

Yume 支持 Apple 官方允许安装并运行 iOS 18 或 iPadOS 18 的全部 iPhone 与 iPad，不另设芯片、内存或具体机型门槛，也不以机型黑名单缩小支持范围。

#### 影响

- 功能、导入、转换和每个公开支持的引擎都必须在最低硬件基线真机上验证；不能只验证 App 壳或资料库。
- 必须针对低内存、低存储、热量和后台时间限制设置受控并发、资源预算及可恢复任务。
- 如果某项引擎能力无法在最低设备上达到产品设定的稳定性与性能阈值，应缩减该能力或优化实现，不能静默排除该设备。
- 具体性能阈值仍需在性能与完成标准决策中量化。

---

<!-- 原文档：docs/decisions/0007-renpy-7-and-8-compatibility-bands.md -->

### ADR-0007：Ren’Py 首版兼容带

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版以 Ren’Py 7.x（Python 2 代际）和 Ren’Py 8.x（Python 3 代际）为目标兼容带。
2. 首版暂不支持 Ren’Py 6.x。
3. “7.x/8.x”是技术验证范围，不等同于承诺每个小版本及所有插件均兼容。对外宣传前必须依据合法测试样本发布精确到已验证版本和能力的兼容矩阵。
4. 两条兼容带均遵守 ADR-0001：运行时随 App 固定提供，不下载、动态加载或执行游戏携带的原生扩展。

#### 影响

- 导入检测必须区分 6.x、7.x 和 8.x；识别到 6.x 时给出明确的暂不支持说明。
- 对使用原生 Python 扩展、平台专用模块或超出受限宿主 API 的游戏，导入扫描应在启动前报告不兼容或兼容性警告。
- 需分别维护 7.x 和 8.x 的解析、执行、存档及回归测试矩阵。

---

<!-- 原文档：docs/decisions/0008-defer-ons-nt2-nt3.md -->

### ADR-0008：首版暂不支持 ONS `nt2` / `nt3`

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版不加入、也不对外宣称支持 ONS `nt2` / `nt3`。
2. 在取得来源合法的格式资料、代表性测试样本和测试授权，并由独立实现通过兼容与安全验证后，方可重新评估支持。
3. 导入扫描遇到 `nt2` / `nt3` 时不得猜测解析或静默降级，应给出“当前版本暂不支持”的明确结果。

#### 影响

- 首版 ONS 兼容矩阵仅列入已有合法资料和样本能够验证的脚本及资源形态。
- UI、商店文案和支持文档不得将 `nt2` / `nt3` 列为首版能力。

---

<!-- 原文档：docs/decisions/0009-defer-kirikiri-custom-encryption-and-plugins.md -->

### ADR-0009：暂不支持 Kirikiri 自定义加密与插件

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版不支持 Kirikiri 自定义 XP3 加密、过滤器、`.tpm` 或其他原生插件。
2. 不尝试破解、绕过或猜测自定义加密，也不加载游戏携带的插件或原生代码。
3. 为后续合法兼容实现保留内部、编译期的策略接口；该接口只接受随 App 构建、审计和签名的实现，不提供运行时插件发现或动态加载能力。
4. 后续新增支持必须拥有合法资料与测试样本，完成许可证、安全和兼容验证，并随新的 App 版本静态发布。

#### 影响

- 导入扫描发现自定义 XP3 过滤器、加密或插件依赖时，应明确报告当前版本不支持。
- 接口设计需将标准归档读取与可选过滤策略分离，但不得暴露通用解密、任意代码回调或动态模块加载入口。

---

<!-- 原文档：docs/decisions/0010-defer-unsupported-media-codecs.md -->

### ADR-0010：暂不支持系统无法原生解码的媒体

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版只直接播放 iOS/iPadOS 系统媒体框架能够稳定解码的容器、视频与音频编码组合。
2. RMVB、RealVideo/RealAudio 以及其他系统无法原生解码的罕见 codec 暂不支持。
3. 首版不为这些格式内置额外解码器，也不在设备端进行解码或转码。
4. 兼容性判断基于容器和实际音视频 codec，不仅依据文件扩展名。

#### 影响

- 导入扫描应读取媒体元数据；不支持的媒体必须在启动前列入兼容报告。
- 播放失败不得使整个导入任务失败，但相关游戏应标记为部分兼容或无法完成对应流程。
- UI 和商店文案不得笼统宣传支持 AVI、MKV、RMVB、MP4 等扩展名。
- 后续新增 codec 支持前必须重新确认实现范围，并完成许可证、专利、包体、性能和安全审计。

---

<!-- 原文档：docs/decisions/0011-dynamic-single-game-import-limits.md -->

### ADR-0011：单游戏采用动态空间与安全限制

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 不为单个游戏设置固定的 GB 容量上限。
2. 导入前必须根据归档声明大小、预计展开大小、派生数据需求、当前可用空间和系统安全预留量计算动态预算；预算不足时拒绝或暂停导入。
3. “无固定容量上限”不取消安全限制。单文件大小、文件数量、路径长度、压缩比、嵌套层数、解析耗时和临时空间均必须设置防滥用上限。
4. 限制应依据设备资源和经过测试的安全预算配置，不得以无限整数、剩余磁盘全部空间或导入失败后依赖系统清理作为实现方式。

#### 影响

- 同一个游戏可能在存储空间充足的设备上可导入，而在空间不足的设备上被安全拒绝。
- UI 必须在导入前显示预计原始占用、临时空间、派生空间和必须保留的安全空间。
- 动态预算公式及各项安全阈值需版本化、可测试并写入导入诊断信息；具体数值在真实归档压力测试后确定。

---

<!-- 原文档：docs/decisions/0012-dynamic-library-capacity.md -->

### ADR-0012：资料库总容量不设固定上限

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 不为游戏资料库设置固定的 GB 总容量上限。
2. 资料库增长受设备实际可用空间和系统安全预留量约束；空间预算不足时禁止开始新的导入或派生转换。
3. 空间不足不得损坏、截断或自动删除现有游戏、存档和原始文件。
4. 缓存与可重建派生文件可由用户明确操作清理；不得把存档视为缓存。

#### 影响

- 设置页必须显示游戏原件、派生文件、存档、缓存和安全预留量的分类占用。
- 导入与转换协调器需要使用统一空间预算，避免多个并发任务分别判断为可执行后共同耗尽磁盘。
- 系统安全预留公式和低空间恢复流程仍需通过真机测试量化。

---

<!-- 原文档：docs/decisions/0013-unlimited-game-count-and-atomic-import-cleanup.md -->

### ADR-0013：游戏数量与导入残留清理

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 不为资料库中的游戏数量设置固定上限。
2. 每次导入必须在任务专属 staging 目录中完成，所有验证和准备步骤成功后才原子提交到资料库。
3. 因空间不足、用户取消、归档损坏、解析失败或 App 恢复后确认任务无法继续时，必须及时清理该任务产生的已复制文件、已展开文件和其他临时数据，避免残留。
4. 清理范围必须绑定任务标识和明确的 staging 路径；不得删除已成功提交的游戏、既有存档或其他任务数据。
5. 若进程被系统终止而无法即时清理，App 下次启动时必须识别未提交任务并执行恢复或清理；清理操作应幂等。

#### 影响

- 资料库列表必须分页或增量加载，索引和搜索不能假设游戏数量有限。
- staging manifest 需记录任务标识、生命周期、文件归属和提交状态，以支持安全的启动时清理。
- 开始导入前的空间预算仍是第一道防线，失败后清理不能替代预检。

---

<!-- 原文档：docs/decisions/0014-dynamic-derived-file-capacity.md -->

### ADR-0014：派生转换文件不设固定容量上限

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 不为单个游戏或整个资料库的派生转换文件设置固定容量上限。
2. 每个转换任务及步骤开始前必须通过统一空间预算预留输入读取、临时输出、最终输出和系统安全空间。
3. 转换输出先写入任务专属临时目录，验证成功后再原子提交为可用派生结果。
4. 因空间不足、取消或错误失败时，只清理本次尚未提交的临时派生文件；不得删除原始游戏、存档或此前已完成且仍被引用的派生结果。
5. App 被系统终止后，应在下次启动时根据转换 manifest 安全恢复或清理未提交结果。

#### 影响

- 转换流程必须使用版本化 manifest 记录输入、临时输出、提交状态和文件归属。
- 设置页应允许用户查看并主动清理可重建的派生文件，但操作前必须明确说明影响。
- 无固定上限不代表无限并发；并发数仍受内存、热量和统一磁盘预算控制。

---

<!-- 原文档：docs/decisions/0015-fully-offline-first-release.md -->

### ADR-0015：首版完全离线

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版完全离线运行。
2. 不提供账号、登录、云端资料库、游戏下载目录或社区内容入口。
3. 不收集或发送遥测、使用分析、崩溃报告或广告标识符。
4. 游戏脚本、插件兼容层和 `WKWebView` 内容不得访问网络；阻止外部导航和非白名单 URL scheme。
5. 诊断日志只保存在本机，只有用户明确发起导出时才生成可分享文件；Yume 不自动上传。

#### 影响

- 首版不得引入依赖远程配置、远程资源、在线许可证校验或服务端功能才能工作的组件。
- 兼容性数据、引擎能力和规则更新只能随 App 版本静态发布。
- 隐私清单、权限声明和 App Review Notes 应与“无数据收集、无联网功能”的实际行为一致。
- 用户主动通过系统分享面板导出诊断文件不属于 App 自动联网，但导出前必须预览数据范围并清除敏感信息。

---

<!-- 原文档：docs/decisions/0016-allow-user-owned-adult-content.md -->

### ADR-0016：允许导入用户合法持有的成人内容

- 状态：已被 ADR-0031 修订
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

> 2026-08-26 修订：本 ADR 中的“成人内容”仅指 ADR-0031 允许的成熟主题内容，不包括露骨色情、违法内容或其他违反 App Store 规则的内容。

1. Yume 允许用户从本地导入自己合法持有、依法可使用且符合 ADR-0031 内容边界的成熟主题游戏。
2. Yume 不内置、托管、推荐、展示或提供此类游戏的下载入口，不提供破解、密钥或绕过年龄与访问控制的能力。
3. 游戏内容始终留在本机；诊断日志、封面缩略图和商店演示不得泄露或使用用户导入的成人内容。
4. App Store 年龄分级、地区合规、内容提示和本地保护方式仍需分别确认，并在提交前按当时有效规则复核。

#### 影响

- 资料库不能依赖自动内容识别准确判定成人内容；如提供标记，应支持用户手动设置并保护隐私。
- App 文案需清楚说明用户对导入内容的权利和合法性负责，同时 Yume 仍应提供适当的本地隐私控制。
- 商店截图、审核 demo 和自动化测试不得使用无明确授权的成人作品。

---

<!-- 原文档：docs/decisions/0017-no-adult-content-label-or-local-lock-in-v1.md -->

### ADR-0017：首版不提供成人内容标记与本地锁

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版不提供单游戏“成人内容”标记。
2. 首版不提供基于该标记的封面或标题隐藏，也不提供 Face ID、Touch ID 或设备密码形式的 App 内本地锁。
3. 不实现自动成人内容扫描或分类，也不宣称能够识别用户导入游戏的内容等级。

#### 影响

- 设置页和游戏详情页不出现成人内容过滤、隐藏或解锁选项。
- 用户需依赖设备与系统层面的访问控制管理设备使用权限；产品文案不得暗示 Yume 提供额外保护。
- App 年龄分级仍需按允许导入的内容范围和提交时有效规则如实申报。
- 后续若加入本地标记或锁定，需重新确认数据模型、隐私、恢复路径和绕过风险。

---

<!-- 原文档：docs/decisions/0018-truthful-age-rating.md -->

### ADR-0018：如实申报年龄分级

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 按 App Store 提交时各上架地区有效的年龄分级问卷与规则如实申报。
2. 申报必须反映 Yume 允许用户导入自己合法持有的成人内容这一产品能力，即使 App 本身不内置、推荐或下载内容。
3. 不通过模糊文案、隐藏能力或选择较低问卷答案规避较高年龄等级。
4. 最终等级由提交时的问卷和平台规则确定；当前设计文档不硬编码可能变化的等级数字。

#### 影响

- 上架前必须逐地区复核年龄等级、成人内容政策和可用性限制，并保存问卷答案与依据。
- App 描述、Review Notes、隐私政策和实际功能必须一致说明内容由用户本地导入。
- 若某地区规则不允许这种能力，应调整该地区的发布范围或产品能力，不得绕过审核。

---

<!-- 原文档：docs/decisions/0019-no-icloud-sync-in-v1.md -->

### ADR-0019：首版不使用 iCloud 同步

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 为保持完全离线，Yume 首版不使用 iCloud、CloudKit 或其他云服务同步游戏、存档、设置、封面或诊断数据。
2. 存档只保存在本机 Yume 容器内，并与缓存及可重建派生文件分开管理。
3. 可提供由用户明确发起的本地存档导出与导入，通过系统文件选择器或分享面板完成；Yume 不自动上传。

#### 影响

- 不实现云端冲突合并、账号绑定或跨设备自动恢复。
- 删除 App 或设备损坏可能导致未导出存档丢失，产品需清楚提示并提供可理解的离线备份流程。
- App 的 iCloud entitlement、CloudKit 容器及相关后台能力不应启用。

---

<!-- 原文档：docs/decisions/0020-no-authorized-engine-samples-yet.md -->

### ADR-0020：当前尚无合法引擎测试样本

- 状态：已确认现状 / 待解决门禁
- 日期：2026-08-26
- 决策人：产品负责人

#### 现状

项目目前尚未拥有可用于开发测试、自动化测试和 App Store 审核演示的各目标引擎合法样本。

#### 门禁

1. 在取得来源清楚、授权范围覆盖相应用途的样本前，不得用商业游戏或来源不明文件替代测试夹具。
2. 没有合法样本的引擎不得被标记为已验证，不得出现在 App Store 兼容能力宣传中，也不得作为审核演示能力提交。
3. 可以继续设计和实现与具体受版权保护内容无关的通用 App 壳、安全存储、任务模型和格式无关基础设施，但引擎支持的完成定义不能通过。
4. 样本不得包含无权再分发的游戏素材、RTP、字体、音频、视频、密钥或破解内容。

#### 已确认的解除方案

立即逐引擎自行制作完全自有的最小夹具，并记录权利证明、内容哈希、覆盖能力和预期结果；只有自制夹具无法合法覆盖的能力才寻求第三方书面授权。夹具必须先于对应兼容实现进入开发与验证流程。

---

<!-- 原文档：docs/decisions/0021-authorized-test-fixture-strategy.md -->

### ADR-0021：合法测试夹具获取策略

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 各引擎优先由 Yume 项目自行制作完全自有的最小测试游戏和不兼容夹具。
2. 自制夹具无法覆盖的功能，再取得第三方权利人的明确书面授权。
3. 授权记录必须说明素材与代码权利、允许的用途、是否可提交仓库、是否可在 CI 使用、是否可随审核包或向 App Review 展示，以及是否允许公开再分发。
4. 未获得对应授权的样本只能由权利人自行保管和测试，不得进入仓库、CI、App 包或审核附件。
5. 每个引擎只有在合法夹具覆盖核心流程、存档、输入和媒体矩阵后，才可通过发布门禁。

#### 影响

- 仓库需维护测试夹具清单、来源、许可证/授权文件、内容哈希和覆盖能力。
- 自制测试游戏的脚本、素材、字体、音频和视频必须全部自有、公共领域或采用经审计的许可兼容资源。
- ADR-0020 的阻塞状态应按引擎逐项解除，不能以一个引擎的样本替代其他引擎验证。

---

<!-- 原文档：docs/decisions/0022-brand-availability-not-verified.md -->

### ADR-0022：品牌可用性尚未核验

- 状态：已确认临时方案 / 待解决上架门禁
- 日期：2026-08-26
- 决策人：产品负责人

#### 现状

“Yume” 名称、图标、域名、相关商标以及 App Store 重名情况目前尚未完成检索和可用性确认。

#### 门禁

1. 完成目标地区的名称、近似商标、App Store listing、域名和图标近似性检索前，不得把 “Yume” 视为最终可发布品牌。
2. 不得在可用性未确认时购买不可退款的品牌资产、投放宣传、提交正式商店元数据或使用可能侵权的图标。
3. 检索结果、地区范围、日期、来源、风险判断和最终名称决策必须记录；必要时交由商标专业人员复核。
4. 引擎名称、厂商 Logo 和游戏作品名称的兼容性表述需另行审核，不能因 App 名称可用而推定可用。

#### 临时方案

在完整核验并记录最终品牌决策前，“Yume” 仅作为内部开发代号使用，不作为最终商店名称、对外宣传品牌或可注册权利的承诺。

---

<!-- 原文档：docs/decisions/0023-public-support-page-not-available.md -->

### ADR-0023：公开支持页面尚未提供

- 状态：已确认现状 / 待解决上架门禁
- 日期：2026-08-26
- 决策人：产品负责人

#### 现状

项目目前没有可公开访问的产品支持页面。

#### 门禁

1. 正式提交 App Store 前必须提供稳定、公开可访问的支持 URL。
2. 支持页面至少应包含受支持系统与设备、准确的引擎兼容矩阵、已知限制、离线导入与存档备份说明、故障排查和联系渠道。
3. 页面不得提供游戏、密钥、破解补丁或规避访问控制的说明，也不得作出“全版本”“全部游戏”等未验证承诺。
4. 品牌名称未核验完成前，不应以尚未确认的最终品牌注册或宣传该页面。

---

<!-- 原文档：docs/decisions/0024-privacy-policy-not-available.md -->

### ADR-0024：隐私政策尚未提供

- 状态：已确认现状 / 待解决上架门禁
- 日期：2026-08-26
- 决策人：产品负责人

#### 现状

项目目前没有可公开访问的隐私政策。

#### 门禁

1. 正式提交 App Store 前必须提供稳定、公开可访问且与实际实现一致的隐私政策 URL。
2. 政策应准确说明首版完全离线、无账号、无遥测、不自动上传诊断，以及游戏、存档和日志在本机的保存与删除方式。
3. 应说明系统文件选择器、用户主动导出诊断或存档、未来功能变更及联系渠道；不得声称“零数据处理”而忽略本机数据处理。
4. App 隐私清单、App Store 隐私问卷、权限描述、产品行为和隐私政策必须相互一致。
5. 后续若新增任何联网、遥测、云同步或第三方 SDK，必须先重新取得产品决策并更新政策与申报。

---

<!-- 原文档：docs/decisions/0025-infringement-contact-not-available.md -->

### ADR-0025：侵权联系渠道尚未提供

- 状态：已确认现状 / 待解决上架门禁
- 日期：2026-08-26
- 决策人：产品负责人

#### 现状

项目目前没有公开的版权投诉或侵权联系渠道。

#### 门禁

1. 正式提交 App Store 前必须提供稳定、可公开访问的侵权联系渠道，并在支持页面和相关政策中清楚列出。
2. 渠道应能接收权利人身份与授权证明、涉事内容定位、权利说明、联系方式和处理请求，并建立确认、复核与回复流程。
3. Yume 不托管用户游戏且完全离线，处理流程应准确说明能力边界；不得承诺能够远程查看或删除用户设备上的本地内容。
4. 投诉记录可能包含个人和权利信息，收集范围、保存期限、访问权限和删除方式需纳入隐私政策。
5. 渠道地址未建立前，不得用个人临时邮箱作为正式商店联系承诺。

---

<!-- 原文档：docs/decisions/0026-license-page-and-source-offer-address-not-available.md -->

### ADR-0026：许可证页面与源码交付地址尚未提供

- 状态：已确认现状 / 待解决上架门禁
- 日期：2026-08-26
- 决策人：产品负责人

#### 现状

项目目前没有公开的第三方许可证页面，也没有用于履行潜在源码提供义务的交付地址。

#### 门禁

1. 引入任何第三方代码或资产前必须建立 SBOM，记录名称、固定版本/commit、来源、许可证、修改、版权声明和传递依赖。
2. 正式发布前必须在 App 内“设置 → 关于 → 开源许可”以及公开页面提供所需许可证文本和署名。
3. 虽然 ADR-0001 禁止 GPL 组件，其他许可证仍可能要求保留声明、提供修改源码、附带 NOTICE 或满足替换/重新链接条件；必须逐项审计，不能假设无需履约。
4. 如果最终依赖产生源码提供义务，必须在发布前建立稳定的公开交付地址和版本对应关系；若没有此类义务，应在审计记录中明确结论，而不是创建虚假源码链接。
5. 许可证材料必须与每个发布版本的实际二进制、资源和构建配置一致。

---

<!-- 原文档：docs/decisions/0027-zip-7z-and-password-protected-archives.md -->

### ADR-0027：首版 ZIP、7z 与密码归档支持

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版必须同时支持 ZIP 和 7z 导入。
2. 两种格式都必须支持用户输入密码处理已验证算法范围内的加密归档；具体加密算法矩阵必须按实际实现和合法测试样本发布。
3. 首版不提供“记住密码”。密码只在当前导入任务内存中保留，不写入 Keychain、磁盘、日志、manifest 或诊断导出；任务完成、失败或取消后立即清除。
4. 密码错误、算法不支持、归档损坏和安全策略拒绝必须给出不同错误，不进行密码暴力枚举。
5. 实现可以使用符合 ADR-0001 的宽松许可证或商业许可组件，但必须冻结版本并完成许可证、传递依赖、内存和安全审计。

#### 影响

- ZIP 与 7z 的普通、加密、错误密码、损坏、超大文件、Zip Slip 和压缩炸弹夹具属于发布门禁。
- UI 不显示保存密码开关；同一归档重新导入或后续任务需要密码时，必须由用户再次输入。
- 不得仅因文件扩展名为 `.zip` 或 `.7z` 就宣称所有压缩与加密方法均受支持。
- RAR 是额外候选能力，不包含在本 ADR 的首版硬要求中，需另行确认范围和实现可行性。

---

<!-- 原文档：docs/decisions/0028-defer-rar-support.md -->

### ADR-0028：首版暂不支持 RAR

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版不加入 RAR 或 RAR5 导入，不论归档是否加密。
2. 导入遇到 `.rar` 或经格式探测识别为 RAR 的文件时，应明确报告当前版本暂不支持，不把它误判为 ZIP/7z 或尝试外部打开。
3. 不采用 RARLAB UnRAR 源码、二进制、DLL 或动态插件。
4. 后续只有在重新确认产品范围，并完成具体实现的许可证、安全、性能和格式兼容审计后才可加入。

#### 依据

当前许可与实现调研保留于 RAR 许可调研（见第 13 节），仅供未来重新评估，不代表已批准依赖或功能。

#### 影响

- 首版归档格式矩阵只包含 ZIP 与 7z。
- UI、支持文档和商店文案不得宣称支持 RAR。

---

<!-- 原文档：docs/decisions/0029-one-time-import-rights-confirmation.md -->

### ADR-0029：首次导入时确认内容权利

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 用户第一次发起游戏导入前，必须确认仅导入自己合法持有且有权使用的内容。
2. 正常情况下只确认一次，之后导入不重复弹出；确认版本与时间仅保存在本机。
3. 声明全文持续可在“设置 → 关于/法律”查看。
4. 只有声明发生实质变化，或本机确认记录被清除时，才重新要求确认。
5. 声明不得暗示 Yume 会验证用户所有权，也不能免除 Yume 对自身分发、品牌和审核材料的合规责任。

#### 影响

- 首次导入状态机在打开文件选择器或开始复制前加入一次性确认门禁。
- 拒绝确认时不读取或复制所选游戏，用户仍可使用设置和帮助页面。
- 本地确认记录不包含导入游戏名称、路径或内容信息，且不上传。

---

<!-- 原文档：docs/decisions/0030-rgss-generations-and-safe-win32api-shim.md -->

### ADR-0030：RGSS 三代与安全 Win32API 兼容层

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版同时以 RPG Maker XP、VX、VX Ace，即 RGSS1、RGSS2、RGSS3 为目标兼容范围。
2. Yume 可以静态实现受限、白名单式的 Win32API 兼容层，按已验证的 DLL 名、函数名、签名和安全语义模拟游戏所需行为。
3. 兼容层不得加载或执行游戏携带的 DLL、Windows PE、原生扩展或其他机器码，也不得使用 `dlopen`、JIT、通用 FFI 或任意符号查找。
4. 兼容层不得向脚本暴露 iOS 原生 API、任意文件路径、网络、进程、隐私权限、Objective-C/Swift 反射或可执行内存。
5. 未列入白名单、参数无法验证或语义不能安全等价实现的调用必须明确报告不兼容；不得伪造成功返回值掩盖问题。

#### 影响

- Win32API 支持按函数而非按 DLL 文件笼统声明，每个函数需要合法夹具、参数边界、线程模型和回归测试。
- 导入扫描需解析 RGSS 脚本中的已知 Win32API 使用，启动前生成兼容报告。
- 白名单实现随 App 静态发布；新增函数必须经过代码审查、威胁建模和 App 更新，不能通过下载规则或插件扩展。
- Apple 当前规则要求 App 自包含，且不得向所承载软件扩展或暴露原生平台 API；提交前需重新核对规则 2.5.2 与 4.7.2：https://developer.apple.com/app-store/review/guidelines/

---

<!-- 原文档：docs/decisions/0031-mature-content-boundary-for-app-store.md -->

### ADR-0031：App Store 版本的成熟内容边界

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人
- 修订：ADR-0016 的“成人内容”范围

#### 决策

1. Yume App Store 版本允许用户导入自己合法持有、依法可使用且不违反 App Store 规则的成熟主题游戏。
2. 不支持露骨色情、违法内容，以及其他超出上架地区法律或 App Store 内容边界的游戏。
3. Yume 不内置、托管、推荐、展示或提供任何游戏的下载入口。
4. 首次导入权利声明应同时说明内容边界；用户需确认导入内容合法且符合该边界。
5. 首版仍不进行自动成人内容扫描，不承诺能够准确识别游戏全部内容；审核演示、内置夹具、商店截图和元数据必须严格位于允许范围内。
6. 年龄分级继续按实际能力与各地区规则如实申报。

#### 影响

- ADR-0016 中“允许成人内容”应解释为本 ADR 所限定的成熟但非露骨色情内容，不再表示无条件允许所有成人内容。
- 支持政策需要提供清晰的内容边界和不兼容说明，但不得声称 Yume 已自动审查用户本地游戏。
- Apple 当前规则 1.1.4 禁止露骨色情内容，4.7.5 对超出 App 年龄分级的软件提出识别和年龄限制要求；提交前必须重新核对：https://developer.apple.com/app-store/review/guidelines/
- 本地导入游戏适用规则 4.7 的具体方式仍存在审核判断空间，应在提交前预沟通并准备详细 Review Notes。

---

<!-- 原文档：docs/decisions/0032-mv-mz-safe-javascript-compatibility.md -->

### ADR-0032：MV/MZ 与安全 JavaScript 兼容层

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. RPG Maker MV 与 MZ 同时作为 Yume 首版目标。
2. 运行环境使用系统 `WKWebView` / WebKit，不打包 Chromium、NW.js 或完整 Node.js 运行时。
3. 支持经验证的浏览器兼容纯 JavaScript 插件，以及受限 CommonJS 模块加载。
4. 可以静态实现白名单式 NW.js/Node API 兼容层，只提供游戏虚拟文件系统、独立存档、输入、音频和其他经过安全审计的等价能力。
5. 不加载或执行 `.node`、DLL、dylib、framework、NW.js 原生模块或其他游戏携带的机器码。
6. 不提供 `child_process`、任意主机文件系统、网络、系统 shell、环境变量、通用原生桥、Objective-C/Swift 反射或隐私权限。
7. 未列入白名单或无法安全等价实现的 API 明确报告不兼容，不伪造成功。

#### 影响

- 导入扫描需要区分浏览器部署内容、NW.js 包装以及插件/API 依赖，启动前生成兼容报告。
- CommonJS 解析只允许游戏虚拟根目录中的纯 JavaScript/JSON 模块，必须防御路径逃逸、循环依赖和资源耗尽。
- 白名单随 App 静态发布；新增 API 需要合法夹具、威胁建模、回归测试和 App 更新。
- Apple 当前规则 2.5.2 与 4.7.2 要求 App 自包含，并限制向所承载软件暴露原生平台 API；提交前重新核对：https://developer.apple.com/app-store/review/guidelines/

---

<!-- 原文档：docs/decisions/0033-ons-v1-format-scope.md -->

### ADR-0033：ONS 首版脚本与资源范围

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版 ONS/NScripter 兼容目标包括 `0.txt`、`00.txt` 和标准 `nscript.dat` 脚本形态。
2. 首版资源归档目标包括经合法样本验证的 NSA、SAR、NS2、NS3 标准形态。
3. `nt2`、`nt3` 继续暂不支持，遵守 ADR-0008。
4. “目标格式”不表示支持所有方言、扩展命令、归档变体或自定义加密；对外矩阵必须列出实际验证范围。
5. 不使用 ONScripter GPL 源码，解析器、执行层和资源读取按 ADR-0001 独立实现或使用经审计的宽松许可证组件。

#### 影响

- 导入检测需区分脚本形态、资源归档、文本编码和方言证据，不能只按扩展名判定。
- 标准格式与自定义加密/访问控制必须区分；后者不得猜测、破解或绕过。
- 每种脚本、归档、编码与错误输入组合需要合法夹具和安全测试。

---

<!-- 原文档：docs/decisions/0034-kirikiri-2-z-v1-scope.md -->

### ADR-0034：Kirikiri 2 / Z 首版范围

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Kirikiri 2 与 Kirikiri Z 同时作为 Yume 首版目标。
2. 首版兼容范围包括经合法样本验证的标准 TJS/KAG 行为，以及未使用自定义加密或过滤器的标准 XP3 资源。
3. 自定义 XP3 加密、过滤器、`.tpm`、原生插件、Windows API 和 DirectShow 依赖继续暂不支持，遵守 ADR-0009。
4. XP3 只是归档格式；单个 `.xp3` 文件不构成完整引擎识别证据，也不代表可运行。
5. TJS 运行在受限兼容层内，不得访问网络、任意文件系统、进程、原生反射或隐私权限。

#### 影响

- Kirikiri 2 与 Z 必须分别维护脚本、KAG、存档、媒体、字体和不兼容特性的验证矩阵。
- 导入扫描需识别标准 XP3 与自定义过滤/加密迹象，在执行前给出明确报告。
- 后续新增过滤器只能作为随 App 静态构建的合法实现，不能加载游戏插件。

---

<!-- 原文档：docs/decisions/0035-save-round-trip-and-offline-transfer.md -->

### ADR-0035：存档往返与离线导入导出

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 每个对外宣称支持的引擎必须在首版通过存档、读档和存档往返一致性测试。
2. 存档按游戏隔离保存，与原始游戏、派生文件和缓存分开；清理缓存不得删除存档。
3. 首版提供单游戏存档的用户主动离线导出与导入，通过系统文件选择器或分享面板完成，不自动上传。
4. 导入存档前必须验证游戏标识、引擎/兼容层版本、文件结构、路径安全和覆盖影响；不兼容时拒绝或要求明确确认，不能静默覆盖。
5. 不承诺未经验证的 Windows、Android、其他模拟器或不同游戏版本之间的跨平台存档兼容。

#### 影响

- 每个引擎的完成定义必须包含新建存档、覆盖、读取、自动存档（若原作具备）、异常退出恢复和导出后重新导入测试。
- 导出包不得包含游戏本体、密钥、日志或与存档无关的用户数据。
- 覆盖现有存档前应提供可恢复备份或原子替换，失败不得损坏原存档。

---

<!-- 原文档：docs/decisions/0036-input-methods-and-per-game-layouts.md -->

### ADR-0036：输入方式与单游戏控制布局

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版支持直接触控、虚拟摇杆/虚拟按键、外接键盘，以及 MFi/系统游戏控制器。
2. 直接触控用于点击、拖动、手势或指针语义，并由各引擎适配器映射为原作输入。
3. 视觉小说与 RPG 使用不同的默认虚拟控制模板；用户调整的位置、尺寸、透明度和布局按游戏独立保存。
4. 所有实体输入由宿主统一接收和映射，引擎脚本不得直接访问 iOS 设备 API。
5. 控制器断开、键盘切换、App 失焦和系统中断时必须安全释放按键状态，避免持续移动或按键卡住。
6. 用户可以为每个游戏自定义键盘按键和控制器按钮映射，并可恢复该引擎的默认映射。

#### 影响

- 每个引擎的兼容矩阵需分别验证触控、键盘和控制器的核心流程。
- UI 必须支持安全区、横竖屏、不同屏幕尺寸、Dynamic Type 辅助设置和 VoiceOver 可达的布局编辑。
- 单游戏控制设置与存档分开保存；重置布局不得删除存档。
- 映射编辑器需检测冲突、保留系统退出/紧急返回路径，并支持在控制器断开时使用触控恢复默认。

---

<!-- 原文档：docs/decisions/0037-defer-midi-support.md -->

### ADR-0037：首版暂不支持 MIDI

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版暂不支持 MIDI 播放。
2. 首版不内置、下载或动态获取 soundfont，也不把 MIDI 静默替换为其他音频。
3. 导入扫描发现游戏核心流程依赖 MIDI 时，必须在启动前列出明确警告；根据影响标记为部分兼容或无法运行。
4. UI 和商店文案不得笼统宣称完整音频兼容。

#### 影响

- RGSS 等使用 MIDI 的游戏可能缺少背景音乐，但音效或其他系统支持的音频仍可按矩阵播放。
- 后续加入 MIDI 前需重新确认产品范围，并完成合成器、soundfont、许可证、包体、性能和音频会话审计。

---

<!-- 原文档：docs/decisions/0038-game-fonts-and-system-fallback.md -->

### ADR-0038：游戏字体与系统回退

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版允许读取用户导入游戏包内自带的 TTF/OTF 字体，仅供对应游戏本地渲染使用。
2. 字体缺失、损坏、格式不支持、注册失败或缺少所需字形时，使用按语言与字符覆盖验证的系统字体回退链。
3. 不从网络下载字体，不加载可执行字体插件，也不随 App 内置来源不明或许可证未经审计的字体。
4. 游戏字体仍视为不可信输入，必须限制文件大小、字体数量、字形/表规模和解析资源，并使用系统安全字体 API。
5. Yume 不将用户游戏字体导出、共享给其他游戏或复制进 App 公共资源。

#### 影响

- 兼容报告需显示实际使用字体、缺失字形和回退情况，不能静默导致乱码或布局不可读。
- 中日韩文本、emoji、组合字符、竖排需求和字体度量差异需加入合法夹具测试。
- 商店 demo 与内置测试夹具只能使用自有、系统或许可证审计通过的字体。

---

<!-- 原文档：docs/decisions/0039-defer-share-extension.md -->

### ADR-0039：首版暂缓 Share Extension

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版只通过 App 内“+”入口和系统文件选择器导入游戏归档。
2. Share Extension 暂缓到后续版本，首版不创建扩展 target、共享 App Group 或从其他 App 分享到 Yume 的入口。
3. 用户仍可在系统“文件”App 中选择本地、外接存储或用户已配置的文件提供器内容；Yume 只读取选择器明确授权的文件。

#### 影响

- 首版导入流程和测试矩阵不包含扩展生命周期、共享容器、后台复制或扩展内密码交互。
- UI 与帮助文档不能展示系统分享菜单导入。
- 后续加入 Share Extension 时需重新确认大文件交接、空间预算、任务恢复、权限和隐私边界。

---

<!-- 原文档：docs/decisions/0040-primary-navigation.md -->

### ADR-0040：首版一级导航

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. Yume 首版一级导航只有“游戏库”和“设置”两个 Tab。
2. “关于”作为“设置”内的页面，不占一级 Tab。
3. 导入与转换任务通过导航栏进度入口进入任务列表；没有活跃任务时入口可以隐藏，不设置常驻“任务”Tab。
4. 兼容性报告从游戏详情、导入结果或设置中的兼容性说明进入，不占一级 Tab。

#### 影响

- iPhone 使用双 Tab；iPad 可采用与两项信息架构等价的侧栏或分栏表现，但不得新增未确认的一级功能。
- 活跃任务必须始终有可发现入口，并通过系统可访问性标签表达进度和状态，不能只靠动画或颜色。
- 深层页面返回后应恢复原 Tab、资料库滚动位置和筛选状态。

---

<!-- 原文档：docs/decisions/0041-app-review-preconsultation-deferred.md -->

### ADR-0041：App Review 预沟通方式（已由后续决策取代）

- 状态：已被第 12.1 节第 5、6 项取代
- 日期：2026-08-26
- 决策人：产品负责人

#### 现状

原决定曾暂缓预沟通；2026-08-26 后续已确认在大规模引擎开发投入前主动向 App Review 预沟通规则 4.7，并采用“先评估最小合规调整、若破坏完全离线与安全底线则停止 App Store 路线”的处理原则。

#### 约束

1. 不得把暂缓决定解释为 Apple 已认可 Yume 的架构、内容范围或规则 4.7 适用方式。
2. 正式提交前至少必须准备详细 Review Notes、完全自有审核样本、软件索引/元数据方案、脚本沙箱说明和无外部原生代码证明。
3. 若后续选择预沟通，应保存问题、材料、Apple 回复、日期和适用版本；非正式回复不自动构成长期批准。
4. 在审核路径明确前，可以继续不依赖该结论的产品与架构设计，但不得对外承诺一定能够通过 App Store 审核。

#### 风险

Apple 当前规则 4.7 涉及 mini games、plug-ins 和 game emulators，并包含内容、原生 API、索引及年龄限制要求。本地用户导入模式的具体适用方式存在审核判断空间：https://developer.apple.com/app-store/review/guidelines/

---

<!-- 原文档：docs/decisions/0042-system-device-backup-scope.md -->

### ADR-0042：系统设备备份范围

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 导入的游戏原件、可重建派生文件、转换临时文件和缓存排除在系统设备备份之外。
2. 用户存档和 Yume 设置允许进入用户自行启用的加密设备备份。
3. 该行为属于操作系统级设备备份，不是 Yume 主动使用 iCloud/CloudKit 同步；ADR-0019 继续有效。
4. 离线手动存档导出仍作为用户可控的主要备份方式。

#### 影响

- 写入各目录后必须正确设置并验证备份排除属性；文件原子替换、移动或恢复后需保持属性。
- 设置页应清楚说明哪些数据可能随系统备份恢复，以及游戏本体需要用户重新导入。
- 恢复后若只有存档而没有对应游戏，应安全保留为待关联存档，不得自动删除或误配。

---

<!-- 原文档：docs/decisions/0043-game-deletion-and-save-retention.md -->

### ADR-0043：删除游戏与保留存档

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 删除游戏前必须分别显示游戏本体、派生文件和存档的占用大小。
2. 用户必须明确选择“同时删除存档”或“保留存档”；不得用含糊的单一删除按钮隐含删除存档。
3. 删除确认页默认选择“保留存档”；只有用户主动改选“同时删除存档”并完成确认后才删除存档。
4. 清理缓存或派生文件永远不得删除存档。
5. 选择保留后，存档进入待关联状态；以后重新导入相符游戏时，必须验证身份与版本后才能恢复关联。
6. 选择同时删除存档前，应提供离线导出入口并再次说明删除后 Yume 内不可恢复。

#### 影响

- 删除事务需要按类别记录目标并保证幂等，部分失败时显示实际保留内容，不能让数据库与磁盘状态静默不一致。
- 待关联存档需在设置的存储管理中可见、可导出和可单独删除。
- 统计大小必须在后台安全计算并允许刷新，不能阻塞主线程。

---

<!-- 原文档：docs/decisions/0044-copy-import-and-immutable-originals.md -->

### ADR-0044：复制导入与不可变原件

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 用户选择游戏后，Yume 始终将所需内容完整复制到自身 App 容器；成功提交后不依赖源文件位置或持续安全范围书签。
2. Yume 不移动、重命名、覆盖、删除或修改用户选择的源文件与归档。
3. 容器内导入副本存放在 `original/`，按不可变原件管理；识别修复、编码转换、媒体处理和兼容补丁只写入 `derived/`。
4. 导入在 staging 完成并验证后原子提交；失败、取消或空间不足时只清理本次 staging，不触碰源文件和既有资料库。
5. 重新导入同一来源时通过内容哈希和证据识别重复项，是否保留两个副本必须由用户决定，不静默覆盖。

#### 影响

- 导入前必须计算源副本、展开、派生、临时和安全预留空间，用户需要为复制成本负责。
- 源文件之后被移动或删除不影响已成功导入的游戏。
- 文件选择器授权只在读取复制期间使用，结束后及时释放。

---

<!-- 原文档：docs/decisions/0045-conversion-consent-threshold.md -->

### ADR-0045：转换确认阈值

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

1. 所有转换都必须非破坏性、可撤销、可重建，并只写入 `derived/`。
2. 耗时较长、明显增加空间占用、可能影响画面/声音表现或包含配置补丁的转换，执行前必须展示步骤、原因、预计时间、预计新增空间和可见影响，并取得用户确认。
3. 轻量、确定性、可撤销且不改变游戏语义的兼容处理可以自动执行，例如生成只读大小写索引、缩略图或检测缓存。
4. 自动处理同样必须写入版本化 manifest，允许查看“处理了什么”并可清除重建。
5. 未经专门验证，不自动改写脚本语义、解密内容或覆盖原件。

#### 影响

- 转换配方需要声明风险等级、用户确认要求、空间估算方式和取消/回滚行为。
- 多个需要确认的步骤应合并为一份清晰计划，避免连续弹窗，同时允许用户查看单项详情。
- 用户拒绝转换时仍保留原件，并显示对应兼容性影响。

---

<!-- 原文档：docs/decisions/0046-discard-staged-source-archive-after-import.md -->

### ADR-0046：成功导入后清理 staging 归档副本

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

ZIP/7z 成功解包、验证并原子提交后，删除 Yume staging 中的源归档临时副本，只保留解包后的不可变游戏原件。用户在 Yume 容器外选择的源归档不受影响。

失败或取消时清理 staging 中的归档副本和展开结果；清理失败则在下次启动时按任务 manifest 重试。UI 应在导入前说明 Yume 内不会额外长期保留压缩包。

---

<!-- 原文档：docs/decisions/0047-duplicate-game-import-handling.md -->

### ADR-0047：重复游戏导入处理

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

检测到相同或高度疑似重复游戏时，不静默覆盖。用户可选择取消、保留两个副本或替换旧版本。替换默认保留原有存档；替换前展示身份、版本、占用和兼容风险，并以可回滚事务提交。

重复判断必须提供内容哈希、引擎标识和根目录证据；低置信度时只能提示疑似重复，不能自动合并。

---

<!-- 原文档：docs/decisions/0048-multiple-games-in-one-archive.md -->

### ADR-0048：单归档多游戏导入

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

一个归档发现多个游戏根目录时，列出所有候选项、引擎证据和空间估算，允许用户一次选择多个并分别导入。每个游戏拥有独立 ID、原件、派生目录、存档、manifest 和失败状态。

单个候选失败不得回滚已成功提交的其他候选，但归档级 staging 只有在所有已选任务完成或终止后才可清理。

---

<!-- 原文档：docs/decisions/0049-user-resolution-for-ambiguous-detection.md -->

### ADR-0049：低置信度识别由用户决定

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

引擎、游戏根目录或文本编码识别置信度不足时，展示候选、证据、警告和乱码预览，由用户明确选择；不得静默采用最高分结果。

用户选择与检测器版本写入 manifest，并允许重新扫描和更改；重新选择只能重建派生结果，不修改不可变原件。

---

<!-- 原文档：docs/decisions/0050-pausable-background-tasks.md -->

### ADR-0050：后台任务安全暂停与恢复

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

导入、解包、哈希和转换在系统允许的后台时间结束前保存检查点并安全暂停；返回前台后自动恢复。产品不承诺无限后台运行，也不以阻止挂起或持续高耗能方式维持任务。

暂停、恢复和取消必须幂等；恢复前重新验证输入、staging、空间预算和配方版本，无法安全恢复时清理本次未提交数据并说明原因。

---

<!-- 原文档：docs/decisions/0051-launch-localizations.md -->

### ADR-0051：首发 App UI 本地化

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

首发同时提供简体中文、繁体中文、英语和日语 App UI 本地化。所有用户可见文本、错误、兼容报告、VoiceOver 标签、隐私/法律入口和审核演示材料均纳入本地化，不以硬编码字符串作为回退。

游戏文本编码与 App UI 语言相互独立；切换 App 语言不得改写游戏原件或擅自改变检测到的游戏文本编码。

---

<!-- 原文档：docs/decisions/0052-library-grid-and-list-presentation.md -->

### ADR-0052：资料库网格与列表视图

- 状态：已确认
- 日期：2026-08-26
- 决策人：产品负责人

#### 决策

资料库默认使用封面网格，用户可切换列表视图。视图偏好按设备本地保存，不同步到云端；iPhone 与 iPad 可分别保持不同选择。

两种视图必须提供相同的搜索、筛选、排序、兼容状态和可访问操作，且对大量游戏采用增量加载与降采样缩略图。

---

<!-- 原文档：docs/research/rar-read-support-licensing.md -->

### RAR 只读支持：许可与能力调研

- 状态：调研结论，待产品确认范围与法律复核
- 核对日期：2026-08-26
- 范围：App 内本地解压，不创建 RAR

#### 结论

首版可以评估通过 BSD 风格许可的 libarchive 加入**只读、非加密 RAR/RAR5 解压**，不需要采用 RARLAB 的 RAR/WinRAR/UnRAR 二进制或源代码。基于所核对条款，使用 libarchive 自身实现进行只读解压未显示需要向 RARLAB 购买运行时许可；这是工程与许可调研结论，不替代商业发布前的法律意见。

不能据此加入 RAR 创建能力或承诺密码 RAR：

- RARLAB 的官方 EULA 明确将 RAR 压缩算法视为专有，并限制利用 RAR、WinRAR 或 UnRAR 代码重建该算法。
- 官方 UnRAR 是带用途限制的 source-available/freeware 许可，不属于本项目允许的 MIT/BSD/Apache-2.0/zlib 类宽松许可证，因此不得引用、修改或分发。
- libarchive 整体及当前 RAR/RAR5 reader 文件使用 BSD 风格条款，官方能力表列出 RAR 与 RAR 5.0 读取，但注明因 RAR 的专有状态存在限制。
- libarchive 当前 RAR5 reader 明确返回“不支持加密”，因此不能用它实现密码 RAR。

#### 建议产品边界

1. RAR 仅作为导入读取格式，不创建或修改 RAR。
2. 首版只支持兼容矩阵验证通过的非加密 RAR/RAR5；加密 RAR 明确报告暂不支持。
3. 不使用官方 UnRAR 源码、二进制、DLL 或动态插件。
4. 若选用 libarchive，必须固定版本、仅静态编入需要的 reader、审计每个实际编译文件与传递依赖，并保留 BSD 声明。
5. 对 RAR 解析器进行 fuzz、路径逃逸、压缩炸弹、多卷、solid archive 和损坏输入测试；libarchive 的历史 RAR reader 安全修复说明该攻击面不可忽视。
6. 最终发布前由合格法律人员复核格式权利、目标地区和具体二进制组成。

#### 一手资料

- RARLAB EULA：https://www.rarlab.com/license.htm
- RARLAB 官方附加组件与 UnRAR 下载：https://www.rarlab.com/rar_add.htm
- libarchive 官方仓库与格式能力：https://github.com/libarchive/libarchive
- libarchive `COPYING`：https://github.com/libarchive/libarchive/blob/master/COPYING
- libarchive RAR reader：https://github.com/libarchive/libarchive/blob/master/libarchive/archive_read_support_format_rar.c
- libarchive RAR5 reader：https://github.com/libarchive/libarchive/blob/master/libarchive/archive_read_support_format_rar5.c

<!-- 决策记录：2026-08-26 负责人口头确认，由代理代为落档 -->

### ADR-0053：放弃 App Store 分发，整体 GPL 化并引入既有引擎实现与用户自带 RTP

- 状态：已确认（负责人 2026-08-26 书面选择）
- 决策人：产品负责人
- 取代关系：本 ADR 取代 ADR-0001（放弃 GPL 组件）、ADR-0002（一次性买断商业模式的闭源前提）中与之冲突的部分；ADR-0030 中"不引用 mkxp-z"的限制解除；ADR-0019/0015 等离线与数据边界不受影响。

#### 背景

RGSS 游戏普遍依赖 RTP 标准素材。原路线（独立实现 + 只运行资源完整游戏 + App Store 闭源首发）意味着 RTP 缺失游戏永远无法运行。负责人在被告知全部后果后选择改走 GPL 开源路线，以换取直接复用既有成熟实现（如 mkxp-z、ONScripter），并为"用户自带 RTP 本地映射"扫清产品边界。

#### 决策

1. Yume 自有代码改为 GPL 许可分发；因 ONScripter 为 GPL-2.0-only，自有代码采用 GPL-2.0-or-later 以保证组合程序可依 GPL-2.0 条款整体发布。
2. 放弃 App Store 与 TestFlight 上架目标（GPL 与 Apple ToS 不兼容）。分发改走：GitHub 公开完整对应源码 + 用户自建签名安装、社区侧载渠道、欧盟备用市场（逐家核对其条款兼容性后再接入）。
3. 允许引入 GPL 及其他许可证的既有引擎实现，逐项完成 ADR、固定版本/commit、SBOM 与安全审计后方可合入：
   - RGSS：mkxp-z（GPL-2.0-or-later）
   - ONScripter：ONScripter 官方实现（GPL-2.0）
   - Kirikiri：krkrsdl2（核心 MIT，依赖逐项审计）
   - Flash：Ruffle（Apache-2.0 OR MIT；使用其非 JIT 解释模式）
   - Ren'Py：评估直接集成 Ren'Py 官方运行时栈（主许可宽松但捆绑组件复杂，须先完成完整 SBOM 审计）
4. iOS 平台硬约束不变：禁止 JIT、动态加载与下载执行代码；上述所选实现均为解释器模式，符合约束；一切仍静态编入单一 App 二进制。
5. RTP 边界修改：Yume 仍不内置、不分发、不下载 RTP；新增"用户导入自己合法持有的 RTP 到本地容器，按引擎/游戏映射"能力。映射规则写入 manifest 并可在诊断中查看。
6. 商业模式暂停：买断制随闭源路线一并中止；后续是否接受赞助/付费自建服务由负责人另行决定，不在本 ADR 范围内。
7. 合规义务不减少：保留第三方许可页、署名、完整对应源码公开（含构建脚本）、书面源码提供方式；发布前仍需专业法律复核（重点变为 GPL 合规与商标）。

#### 后果

- 原"App Store 首发""外部 TestFlight"里程碑作废，替换为"开源合规发布"门槛：SBOM 完整、全源码可复现构建、许可页齐全、法律复核通过。
- 引擎开发策略从"逐项独立实现（夹具先行）"转为"逐项上游集成（审计先行）"：每引入一个上游实现，先冻结 commit、审计许可证与传递依赖、验证 iOS 真机性能，再进入兼容矩阵阶段。
- 夹具要求不变：任何引擎宣称支持前仍需合法测试集验证核心流程与存档往返。
